# vocpage — Session Progress

## 다음 세션 시작점

→ **`/voc` 단일 PR `docs/voc-completion-single-pr` PR open 대기**
   머지는 사용자. 머지 후 Wave 2(Dashboard) 진입.

→ **PR 본문 = `docs/voc-completion-single-pr` 9 commit + driver D-1~D-5 PASS evidence + 머지 요청 1줄**
   - Phase B (BE master + create 강제룰) — `ad1ef50`
   - Phase C minimal + major (모달 200자 카운터·priority lock·2×2 grid·cascade·body skeleton·첨부 sync·auto-tag·send-icon) — `c56af67` + `b85240a`
   - Wave 1.6 잔여 (SidebarUserSwitcher · UploadZone thumb · ActivityTimeline · ErrorSimSelector · Sidebar/Topbar 폴리시) — `dc4527c`
   - Phase 6 (payload-review BE/FE + Playwright spec) — `a8c08ca` + `64069e2`
   - Follow-up 7건 (status icon · 토큰화 · 13.5px · ReviewStatus · collapse test · data-testid slug) — `d266bd7`
   - Code-review fix B-1 / H-1 / H-2 (errorSim 격리 · 권한 헬퍼 통일 · INSERT+UPDATE 트랜잭션) — `97e002c`

→ **D-1~D-5 evidence (driver `voc-completion-driver.md`)**
   - D-1 기능: §2.2 사용자 흐름 / §2.3 처리 lane / §8.16 Result Review / §13.3 등록→검토→완료 1바퀴 — 코드 PASS
   - D-2 시각: token-only / spec 정합 — `rg` hex/oklch 외부 0건
   - D-3 테스트: FE 469/469 PASS · BE 111/118 PASS (+7 todo) · FE lint clean · BE+FE typecheck clean · Playwright spec 작성(실행은 docker compose 기동 필요)
   - D-4 무결성: shared/contracts ↔ openapi ↔ BE 응답 정합 / `scripts/check-fixture-seed-parity.ts` PASS / prototype/ 활성 인용 0건
   - D-5 운영: `docs/specs/reviews/voc-completion-decisions.md` append, 본 progress + next-session-tasks 동기화

## Active Waves

**`/voc` 완성 — 단일 PR (PR open 대기)**
- ✅ 9 commit, BE/FE green, code-review FIX-THEN-MERGE 지적 모두 처리
- ⏳ 사용자 PR open + merge → Wave 2 진입

**Wave 2 — Dashboard** (정본: `docs/specs/requires/dashboard.md`)
- ⛔ `/voc` 완성 PR 머지 후 진입

## 후속 (Medium/Low — 본 PR 외 follow-up bucket)

- M-1 `computeInitialDueDate` Date.now() injectable 파라미터 (테스트 flake 회피)
- M-2 `PayloadReviewActions` mutation onError 사용자 피드백
- M-3 `decision: 'approve'|'reject'` enum 변경 — changelog 표기
- L-1 `VocErrorSimSelector` 멀티탭 동기화
- L-2 `VocActivityTimeline` 정렬 secondary key
