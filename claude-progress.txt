# vocpage — Session Progress

## 다음 세션 시작점 (2026-05-10 Wave 2 Phase E 머지 완료 · ADR 0006/0007 + Wave 3 구현 PR OPEN)

→ **FU-026 ADR 0006 잠금 + Wave 3 Phase A 구현 (PR #310/#311, OPEN, 사용자 머지 대기)**:
   - **PR #310 — ADR 0006 Accepted + 0007 Proposed** (`docs/adr-custom-date-range`, commits `3d23db2`/`24c94e0`):
     - **0006 (Accepted 2026-05-10)**: `dashboard_settings.default_date_range='custom'` 의 `custom_start_date / custom_end_date DATE NULL` 신설 + CHECK (`'custom'` 시 양쪽 NOT NULL+start≤end / 그 외 양쪽 NULL). codex:rescue 적대적 리뷰 반영해 OQ 5건 잠금:
       · OQ-1 (rogue row 백필) = abort 유지 + 운영 수동 절차 (옵션 A `'1m'` 복귀 / 옵션 B row 삭제) ADR §3 명문화. 자동 일괄 coerce 거부.
       · OQ-2 (range 상한) = DB CHECK / zod refine 미적용 (`'all'` 정합). UI picker 5y soft cap 만.
       · OQ-3 (Admin 행 시맨틱) = `'custom'` 차단 유지 (BE 415 `ADMIN_CUSTOM_NOT_SUPPORTED` + UI tooltip). 상대 offset 은 NextGen.
       · OQ-4 (timezone) = ADR 0007 로 분기. 본 ADR 은 DATE + ISO date 직렬화까지만, 단일 timezone 운영 가정.
       · OQ-5 (적대적 리뷰 추가, invariant 위반) = BE 응답 직전 검증 + 500 + 구조화 로그. fallback 강등 거부.
     - **0007 (Proposed)**: `custom` 날짜 범위의 timezone 해석 시맨틱 OQ 4건 (TZ-OQ-1~4). 본 ADR 잠금 전 다중 timezone 운영 진입 금지.
   - **PR #311 — Wave 3 Phase A 구현** (`feat/wave-3-custom-date-range`, base = `docs/adr-custom-date-range`, commit `393d588`):
     - 마이그 023 (rogue row 가드 + 두 컬럼 + CHECK).
     - shared/contracts/dashboard: `entity.ts` superRefine cross-field invariant + `io.ts` 두 옵셔널 컬럼.
     - shared/openapi.yaml 두 컬럼 동기 (date format, nullable).
     - BE `services/dashboard/settings.service.ts`: 'custom' 외 enum → dates auto NULL clear, merged invariant 검증 → 400 (`CUSTOM_DATES_REQUIRED` / `CUSTOM_DATES_RANGE_INVALID`), Admin 행 'custom' → 415 (§7), 응답 invariant → 500 (§6).
     - FE `CustomDateRangePicker.tsx` (react-day-picker `mode="range"`, 5y soft cap, 2-month view) + `DateRangeSection` 추출 + Dialog `'custom'` enable + dates 미입력 시 저장 disable + scope='admin' tooltip 차단 + enum 변경 시 dates auto NULL clear.
     - MSW handler 의 admin 'custom' 415 분기.
     - Tests: BE 593 pass (+6 ADR 케이스 — custom 정상/dates 필수 400/range 역전 400/auto-clear/admin 415/invariant 500), FE 661 pass (Phase E disabled-radio 테스트를 enable+save-disable+auto-clear 시맨틱으로 갱신). FE/BE typecheck/lint 모두 0.
   - **오케스트레이션 메모**: 사용자 지시 "결정 전부 적대적 리뷰어와 토론" → `codex:codex-rescue` 로 OQ 4건 적대적 리뷰. 추천 4건 중 OQ-2 채택 / OQ-4 강한 안 (별 ADR 분리) 채택 / OQ-1·3 부분 반영 / OQ-5 신설. 이후 "그냥 다 정리하고 완성까지" 지시 → 추가 grill 없이 일괄 구현.
   - **다음 세션 액션**: PR #310 머지 → PR #311 base 자동 전환 (또는 rebase) → CI green → `gh pr merge 311 --merge --delete-branch` → 운영 DB 마이그 023 적용 (rogue row 진단 SQL 선행) → 통합 검증 (Dialog 에서 'custom' 선택 + 날짜 입력 + 저장 + 새로고침 picker 값 round-trip).

→ **Wave 2 Phase E — DashboardSettingsDialog (PR #308/#309 머지 완료 2026-05-10)**:
   - **커밋 1 — Base settings panel** (`8d3bbc6`): Sheet 우측 슬라이드인 + 위젯 표시 8 토글 + 기본 날짜 범위 라디오 (1m/3m/1y/all; `'custom'` 은 disabled radio 로 round-trip 보존) + 히트맵 X축 라디오 (status/priority/tag) + Save/Reset/Cancel + close-while-saving guard. `DashboardShell` 가 `hiddenWidgetIds` 받아 children 과 `layouts[bp]` 둘 다 lockstep 으로 필터 (RGL key invariant). 적대적 리뷰 P0×2 + P1×2 close (RGL key alignment, close race, hiddenWidgetIds memoize, draft-driven custom radio).
   - **커밋 2 — Item 1 (admin-default scope toggle) + Item 2 (GlobalTabs reorder)** (`ca06321`):
     - **Item 1**: BE `GET /api/dashboard/settings?scope=admin` (admin only, raw admin-default row via `service.loadAdminDefault`). 403 `FORBIDDEN_ADMIN_SCOPE` for non-admin. FE `useDashboardSettings(scope)` / `useUpdateDashboardSettings(scope)` 가 `'self' | 'admin'` 받음. queryKey scoped, mutation success 가 두 scope 모두 invalidate. Dialog: `저장 대상` 토글 (`내 설정` / `기본값 (Admin)`) `user.role === 'admin'` 일 때만 표시. scope 전환 시 hooks 가 새 scope 로 re-key.
     - **Item 2**: 신규 `GlobalTabsEditor.tsx` — native HTML5 DnD reorder + 행별 visibility 토글 + systemId 추가/제거 + empty state. 신규 dep 없음. `scope === 'admin'` 일 때만 표시. Save patch 가 admin scope 에서만 `globaltabs_order` 포함.
   - **Item 3 — `'custom'` 날짜 picker 의도적 미포함**: `dashboard_settings` 스키마에 `custom_start_date / custom_end_date` 컬럼 부재. 사용자 정의 범위를 *기본값* 으로 저장하려면 마이그 신설 필요. CLAUDE.md irreversibility 게이트 (DB 스키마 변경 ≥90% 신뢰 + 명시 승인) 적용 → FU-bucket (`FU-026`) 등록. base 의 disabled-custom-radio 는 기존 `'custom'` 값 silent loss 방지로 유지.
   - **오케스트레이션 메모**: 사용자 직접 지시 "codex 사용 금지". 적대적 결정/리뷰는 `general-purpose` 에이전트로 대체. 두 라운드 (스코프 결정 Q1/Q2/Q3 + 코드 리뷰 P0×2/P1×2) 모두 일반 에이전트로 처리, 결과 수용/반박 포함.
   - **검증 (PR #308 HEAD)**: BE typecheck 0 / 587 pass (+3) · FE typecheck 0 / 661 pass (+13: base 9 + admin 4) · lint 0 / token lint 0.
   - **다음 세션 액션**: PR #308 코드/UI 확인 → CI green 시 `gh pr merge 308 --merge --delete-branch` → 머지 후 본 progress + `next-session-tasks.md` 동기화.

→ **Wave 2 Phase C — 7 endpoint BE + 7 위젯 FE 머지 완료** (PR #304/#305/#306, 2026-05-10):
   - **PR #304 — vite proxy 포트 정합**: BE `process.env.PORT ?? 3000` → `?? 3001` (vite proxy 가 `localhost:3001` 으로 forward; docker compose 는 컨테이너 내부 PORT=3000 명시 유지). 로컬 dev 페어링 정합.
   - **PR #305 — Phase C BE (단일 PR)**: 7 endpoint (`/api/dashboard/{distribution,priority-status-matrix,heatmap,weekly-trend,processing-speed,assignee-stats,aging-vocs}`) + `dashboard-phase-c.{route,service,repo}.ts` + zod 컨트랙트 7종 (`shared/contracts/dashboard/*.ts`) + 권한 매트릭스 (admin/manager/dev 200, user 403). codex:rescue 적대적 리뷰 P0×2 (KST 캘린더 캐스팅 SQL — `(status_changed_at AT TIME ZONE 'Asia/Seoul')::date - (created_at AT TIME ZONE 'Asia/Seoul')::date`) + P1×1 (`range=custom` 시 `startDate`/`endDate` 누락 → 400; `DashboardFilter` 에 `.refine` 추가, `DashboardFilterBase` 분리하여 5개 child 확장 보존) 픽스 동봉. BE 518 → 584 pass (+66).
   - **PR #306 — Phase C FE (단일 PR)**: 7 위젯 (`{Distribution,PriorityStatusMatrix,Heatmap,WeeklyTrend,ProcessingSpeed,AssigneeStats,AgingVocs}Widget.tsx`) + `GridTable.tsx` 공용 (heatmap / matrix / assignee 3-widget 강도 그리드 — `oklch(63% 0.19 258 / 0.06~0.62)` 선형 보간) + `buildVocUrl.ts` (5 위젯 click-through nav, globalFilter 컨텍스트 보존) + recharts@^3.8.1 (LineChart 3-line 트렌드, PieChart 도넛 분포) + MSW 7 핸들러 (`mocks/handlers/dashboard-phase-c.ts`). codex:rescue 적대적 리뷰 P0×3 (matrix dead code → `dist-matrix` 슬롯이 분포+매트릭스 vertical stack; click-through nav 5위젯 미구현; SLA 토큰 `chart-emerald/amber` → `status-green/amber/red`) + P1×4 (heatmap 빈 카피 `해당 기간 데이터 없음`; 3-level breadcrumb (`전체 › 채널 A › 메뉴 1`); AgingVocs dim toggle 시스템별↔메뉴별 globalTab 연동; AssigneeStats `isHighlighted={r.id === filter.assigneeId}`) 픽스 동봉. FE 603 → 648 pass (+45).
   - **Spec amendment** (`dashboard.md §5 주간 트렌드` v3.1, 2026-05-10): `inProgress` 의미를 "주말 스냅샷 기준 검토중+처리중 상태 VOC 수" → "해당 주에 검토중 또는 처리중으로 전환된 VOC 수" 로 정정. `voc_status_history` 테이블 부재로 진정한 스냅샷 불가 → 진입 카운트로 수렴 (사용자 결정 옵션 A). 컨트랙트 필드명 `inProgress` → `enteredInProgress`.
   - **오케스트레이션 메모**: FE 픽스 1차 시도가 codex:rescue Skill 위임 후 9분 정체 → `TaskStop` → "Skill 호출 금지" 명시한 직접 작업 에이전트로 재디스패치 → 정상 완료. codex 위임은 read-only sandbox 한계로 write-heavy 작업에 부적합.

→ **사이드바 admin 메뉴 누락 픽스** (PR #302 머지, 2026-05-10):
   - 라우터·페이지·API·MSW·테스트는 W3-6 (외부 마스터) / W3-7 Phase E (사용자) 시점에 모두 머지됐는데 `Sidebar.tsx` `ADMIN_NAV_ITEMS` 등록만 누락 → admin 사용자가 메뉴 진입 불가 상태였음.
   - 추가: `/admin/masters` (외부 마스터, admin/manager/dev — `adminGroup: true`, Database icon) + `/admin/users` (사용자, admin only — `adminStrict: true`, Users icon).
   - 순서: 마스터 데이터 (tags → masters) → admin-only ops (users → trash). 검증: typecheck 0 / FE 603 pass.

→ **스테일 브랜치 일괄 정리** (2026-05-10):
   - **PR close 4건** (#291–#294, Wave 2 stacked-PR 계획 폐기분): docs/wave-2-dashboard-plan, chore/wave-2-rgl-install, migration/022-locked-widgets, feat/wave-2-contract. 사유: spec 변경 (locked_widgets → locked_fields 재설계, 022 번호 재할당) · 다른 형태로 main 반영 (RGL 설치, contract io.ts 재설계, dashboard plan 재작성). main 우회 경로 = Phase D #295 / Phase B #297·#298.
   - **원격 브랜치 삭제 12건**: 위 4 unmerged + 머지 완료 8건 (docs/wave-3-phase-a-close, feat/w3-4-tag-master, feat/w3-5-trash, feat/wave-1-6-eta-batch, fix/fu-001-dompurify, fix/fu-002-notice-faq-indexes, fix/sidebar-admin-nav-missing-items, refactor/page-frame-primitives). 로컬 stale branch 5건도 삭제 (chennai 포함). 원격/로컬 모두 main 만 남음.

→ **PageFrame 아키텍처 도입** (PR #301 머지, 2026-05-10):
   - **원인 진단 — 더블 스크롤**: AppShell `<main>` 의 `overflow-auto` 와 `BodyScrollLayout` 본문 `overflow-auto` 가 동시 활성. 대시보드는 RGL 이 명시 height 를 주입해 main 이 함께 스크롤바를 띄움.
   - **아키텍처 — 2-레이어 프리미티브 합성**: `PageFrame` (AppShell↔페이지 경계 유일 계약, `absolute inset-0 flex flex-col`) + 4 프리미티브 (`Sticky` / `Sticky variant=toolbar` / `Scroll` / `SplitRow` / `SidePanel`). 페이지는 flex chain 책임 없음 → 합성만.
   - **`StickyHeaderLayout` sugar (이름 변경)**: 기존 `BodyScrollLayout` 을 PageFrame 합성으로 재작성. 외부 시그니처 동일 → 8 페이지 무변경 유지. 이름 변경 이유: body 만 스크롤하려면 헤더를 형제로 받아야 하므로 본질은 "헤더 sticky 레이아웃".
   - **`<main>` invariant**: `overflow-auto` → `overflow-hidden`. 모든 페이지가 PageFrame 으로 main 패딩박스를 채우므로 main 은 절대 스크롤바를 띄울 수 없음 → 더블 스크롤 구조적 차단.
   - **`PageLayout` 삭제** + 라우터 스텁 2개(StubPage / HealthPage) → `StickyHeaderLayout` (EmptyState 한 줄, overflow 발생 X).
   - **VOC 마이그**: 인라인 `flex flex-col h-full` + 수동 `overflow-hidden` row 체인 제거. `Sticky × 3 (Topbar / Status / Advanced) + SplitRow ⊃ Scroll(padded=false) + VocSidePanel`. 거동 100% 보존.
   - **MSW dashboard 핸들러** (`/api/dashboard/{settings,summary}`): 별도 commit.
   - **검증**: typecheck 0 / FE 603 pass / lint 0 / 토큰 lint 0.

→ **다음 세션 작업** (우선순위 순):
   1. **Phase E** — DashboardSettingsDialog (위젯 토글 + default_date_range + heatmap x-axis + admin lock).
   2. 운영/배포 phase (`connect-pg-simple` / OIDC / production Dockerfile / Playwright E2E).

→ **마이그 번호** — 다음 = **023**.

---

## 이전 세션 시작점 (2026-05-10 Wave 2 Phase B 머지 완료 — 사용자 피드백 대기 → Phase C/E)

→ **Wave 2 Phase B KPI Volume + Quality 머지 완료** (PR #297 BE + PR #298 FE, 2026-05-10):
   - **BE (PR #297)**: `GET /api/dashboard/summary` (8 KPI = Volume × 4 + Quality × 4). `dashboard-metrics.repo.ts` (5 SQL: countCreatedAndCompleted / snapshotUnresolved / snapshotUrgentHighUnresolved / snapshotOverdue14d / avgResolutionDaysInWindow) + `services/dashboard/summary.service.ts` (KST midnight helpers, `default_date_range` fallback user → admin → '1m', custom-range validation, 전주 동기간/동시점 delta 산출) + `routes/dashboard-summary.ts` (auth + `requireRole('dev','manager','admin')` — User 403). `index.ts` 마운트.
   - **Contract (PR #297)**: `shared/contracts/dashboard/summary.ts` (zod) — `KpiMetric { value, delta, delta_kind }` (delta_kind enum: percent/count/days/percentage_point) + `DashboardSummary` + `DashboardFilter` (.strict). `shared/openapi.yaml` `DashboardSummary` 스텁 정형화 + `dashboardRange` parameter 신설 (1m/3m/1y/all/custom).
   - **FE (PR #298)**: `features/dashboard/model/dashboardFilter.tsx` (`DashboardFilterProvider` + `useDashboardFilter`) · `model/useDashboardSummary.ts` (TanStack, queryKey embeds filter → 자동 invalidation, staleTime 30s) · `widgets/KpiCard.tsx` (delta_kind 별 포맷 + inverted 평균처리시간 + accent 보더 delta-driven `color-mix(in oklch, var(--chart-red) 35%, transparent)`) · `widgets/KpiVolumeWidget.tsx` + `KpiQualityWidget.tsx` (loading skeleton + error retry + aria-busy/role=status) · `DashboardShell` `kpi-volume`/`kpi-quality` 슬롯 실제 위젯 교체 (편집 모드는 placeholder fallback) · `DashboardPage` `<DashboardFilterProvider>` wrap.
   - **codex:rescue 적대적 리뷰 (BE)**: P1 2건 + P2 1건 close. P1-1 = `snapshotUnresolved` as-of 정합 (status_changed_at > at OR NOT_DONE) — 그 시점엔 미해결이었지만 후에 완료된 VOC 누락 차단. P1-2 = 스냅샷 KPI는 KST 자정이 아닌 exact `now` 인스턴트 기준. P2 = `resolution_rate` 분모를 period-end snapshot 으로 정합 (numerator/denominator window-consistent).
   - **codex:rescue 적대적 리뷰 (FE)**: P1 1건 + P2 2건 close. P1 = accent 보더는 `value > 0` 이 아닌 `delta > 0` (spec §1 "증가 시 강조"). P2-1 = `aria-busy` + `role="status"` + `aria-live="polite"` loading 영역 a11y. P2-2 = `vi.mock(factory, importOriginal spread)` 로 향후 re-export 보존.
   - **메트릭**: BE 487 → 518 (+31), FE 587 → 599 (+12). typecheck 0 / lint 0 / 토큰 lint 0.

→ **다음 세션 작업**: 사용자 피드백 수집 후 Wave 2 잔여 phase 진입:
   - **Phase C (분포 / 매트릭스 / 히트맵 / 트렌드 / SLA / 담당자 / Top10)** — 7 endpoint BE 단일 PR + 7 위젯 FE PR.
   - **Phase E (설정 패널)** — `DashboardSettingsDialog` (widget toggle + default_date_range + heatmap x-axis + admin lock).
   - **운영/배포 phase** — `connect-pg-simple` / OIDC / production Dockerfile / Playwright E2E.

→ **마이그 번호** — 다음 마이그 번호 = **023** (Phase B 마이그 추가 없음).

---

## 이전 세션 시작점 (2026-05-10 Wave 2 Phase D 머지 완료 — Phase B/C/E 진입)

→ **Wave 2 Phase D RGL Shell 머지 완료** (PR #295, 2026-05-10):
   - **BE**: `routes/dashboard-settings.ts` + `services/dashboard/settings.service.ts` + `repository/dashboard.repo.ts` 신규. `GET/PUT /api/dashboard/settings` (auth required, per-field merge admin∘user). 권한: `scope=admin` + `locked_fields` + `globaltabs_order` 모두 admin-only (3종 별 403 코드).
   - **FE**: `features/dashboard/*` (defaultLayouts × 8 widget × 5 breakpoint, lockMerge 순수 함수, useDashboardDraft state machine + saveCounter race guard, useDashboardSettings + useUpdateDashboardSettings TanStack hooks, DashboardShell ResponsiveRGL, WidgetPlaceholder, EditModeToggle), `pages/DashboardPage` + lazy 라우트 (StubPage 교체), `dashboard-rgl.css` + 토큰 3종 (`--dashboard-grid-margin/RowH/HandleSize`).
   - **Contract**: `shared/contracts/dashboard/{entity,io}` (zod) + `shared/openapi.yaml` `DashboardSettings` 정형 schema + `RglLayoutItem.strict()` + `RglLayouts` 모든 breakpoint optional. `DashboardSettingsUpdate.strict()`.
   - **Migration 022**: `dashboard_admin_default_unique.sql` — Postgres NULL UNIQUE 맹점 차단 (partial unique index `WHERE user_id IS NULL`).
   - **codex:rescue 적대적 리뷰**: P0 1건 + P1 5건 + P2 2건 모두 close.
   - **메트릭**: BE 462 → 487 (+25) 그린, FE 575 → 587 (+12) 그린, typecheck 0, lint 0.

→ **다음 세션 작업**: Wave 2 잔여 phase 진입 후보:
   - **Phase B (KPI Volume + Quality)** — `/dashboard/summary` BE + KPI 위젯 콘텐츠 FE.
   - **Phase C (분포 / 매트릭스 / 히트맵 / 트렌드 / SLA / 담당자 / Top10)** — 위젯별 BE 엔드포인트 + 콘텐츠.
   - **Phase E (설정 패널)** — widget toggle + default_date_range + heatmap_x_axis editor.
   - **운영/배포 phase** — `connect-pg-simple` / OIDC / production Dockerfile / Playwright E2E.

→ **마이그 번호** — 다음 마이그 번호 = **023**.

---

## 이전 세션 시작점 (2026-05-10 P2 후속 batch 머지 완료 — Wave 2 또는 운영/배포 phase)

→ **P2 후속 3건 머지 완료** (2026-05-10):
   - **FU-023** (PR #287): `createTag` slug 파생을 `<name>-<kind>` 로 변경 — FU-014 (name, kind) UNIQUE 와 정합. `('공통','general')` / `('공통','menu')` 둘 다 distinct slug. admin-tags.sql.test.ts `it.todo` → 실 테스트.
   - **FU-024** (PR #289): `@testcontainers/postgresql` 도입 + `__tests__/helpers/testPostgres.ts` (pgvector pg16 컨테이너 + 21 마이그 Up 적용). `admin-sql-tc.test.ts` 5 cell — pg-mem 한계 (ILIKE ESCAPE / RETURNING correlated subquery) 우회. 4 skip 모두 제거.
   - **FU-025** (PR #288): Notice `visible_from/to` KST 자정 정규화 — `NoticeFormDialog.tsx` 에 `toKstMidnightInstant(dateStr)` helper 추출, `+09:00` 직렬화. 회귀 테스트 5 case.

→ **메트릭**: BE 462/462 그린 (skip 4 → 0). FE 575/575 그린. typecheck 0.

→ **다음 세션 작업**: 활성 Open follow-up 다 닫혔음 (P3 만 잔존: FU-009/012/019/020). 진입 후보:
   - **Wave 2 (Dashboard)** — `dashboard.md` plan 작성부터. KPI 8종 + 분포 4탭 + 우선순위×상태 매트릭스 + 히트맵 + 트렌드 + 처리속도 등 §11.7 엔드포인트 구현.
   - **운영/배포 phase** — `connect-pg-simple` 세션 스토어 / OIDC 실구현 / production Dockerfile / 실 MSSQL 연동 / Playwright E2E 등.
   - **Wave 2 권장** (운영/배포는 외부 의존 큼; 기능 완성도 우선).

→ **마이그 번호** — 다음 마이그 번호 = **022**.

---

## 이전 세션 시작점 (2026-05-10 Wave 5 Phase B PR-2 코드 완료 — 사용자 검토 / PR 대기)

→ **Wave 5 Phase B PR-2 코드 완료** (`feat/wave-5-phase-b-fe`, 2026-05-10).
   - **변경 요약**: `<NavItemCountBadge>` 추상 신설 · Sidebar `/notifications` `/notice` `/faq` nav-item 일괄 적용 · notifications API/훅 (`useNotificationsList` + `useUnreadBadge` 30s 폴링, ETag 헬퍼 `etagFetch.ts` 부수 보유) · `/notifications` 페이지 신설 (StubPage 대체, mount 시 mark-all-read) · comments BE wiring (POST/PATCH/DELETE 추가, `useAddComment`/`useUpdateComment`/`useDeleteComment` 훅) · VocComment Textarea → ToastBodyEditor (`format='html'`) + SafeHtml(DOMPurify) 렌더 · FU-006 `var(--border)` 직접 소비자 4곳 정리.
   - **W5-D7 contract gap 결정 (사용자 2026-05-10)**: BE `unread-count` 응답에 `hasUrgent` 미포함 → Sidebar 🔴! 산출 위해 `list` 엔드포인트를 30s 폴링으로 전환. `useUnreadBadge` 가 동일 캐시에서 count + hasUrgent 도출. 두 쿼리 키 결정은 panel UI 캐시 분리로 재해석.
   - **검증**: FE typecheck 0 · 570 tests pass · lint 0 · lint:tokens 0 · BE typecheck 0 · 304 tests pass.
   - **followup-bucket**: FU-006 / FU-008 Closed, FU-009 사용자 ad-hoc 보류 (plan §2.2 정책 정합).
   - **다음 세션 작업**: 사용자 코드 검토 → push → PR 생성 → 머지. 머지 후 시각 baseline (FU-009 잔여) ad-hoc 캡처.

## 이전 세션 시작점 (2026-05-10 Wave 5 Phase A 머지 완료 — Phase B 진입)

→ **Wave 5 Phase A 머지 완료** (PR #276, merge `75b1b0b`, 2026-05-09T15:25Z). Phase B 진입.
   - **Phase A 머지 내용**: notifications BE (repo/service/route + ETag/304/lazy trim/5분 디바운스) + comments BE 신설 (W5-D11, 4 endpoint + 15 test) + voc.ts 트리거 wire (status_change/assigned/comment) + spec §8.6/§8.13 정합화. BE 28 suites · 304 passed · typecheck 0.

→ **다음 세션 = Wave 5 Phase B PR-2 (FE wiring + P2 FE 흡수 batch)**.
   - **범위**:
     1. **FE notifications**: bell + dropdown (Sidebar 배치 W5-D1) + 30s 폴링 + ETag/304 / visibilityState 일시중단 / 401 → 로그인 리다이렉트 (spec §8.14). 두 쿼리 키 분리 (W5-D7).
     2. **FE comments UI**: `VocComment.tsx` 의 `<Textarea>` → `<ToastBodyEditor>` 교체. POST/PATCH/DELETE 훅 wire. DOMPurify 렌더 (spec §8.13).
     3. **`<NavItemCountBadge>` 추상** (W5-D8): 알림 / Notice / FAQ unread count 단일 컴포넌트. **FU-008 흡수**.
     4. **FU-006 흡수**: shadcn token drift (`--border` / `--danger` 잔존) 정리.
     5. **FU-009 흡수**: visual-diff baseline 추가 (bell dropdown / 댓글 에디터 surface).
   - **결정 잠금 W5-D1~D11** 유지. P2 BE batch (FU-007/010/015/018) 은 별 PR — 다음 세션 범위 밖.
   - **세션 회고 (2026-05-10)**: Phase A 코드 완료 → 사용자가 위임 → push + PR-1 + 머지까지 자동 진행 (PR #276). 위임 패턴 = trivial 직접 + 큰 덩어리 sub-agent + 검증/문서/git 자동.

→ **마이그 번호** — 다음 마이그 번호 = 021 (변동 없음).

→ **마이그 번호** — 다음 마이그 번호 = 021 (변동 없음).

## 이전 세션 시작점 (2026-05-09 Wave 5 plan 작성 — 알림 + Sidebar nav count badge)

→ **Wave 5 plan 작성 (docs-only, 2026-05-09)** — `docs/specs/plans/wave-5-notifications.md` 신설.
   - **범위**: 인앱 알림 (§8.6 / §8.14 — 벨 / 미읽음 배지 / 🔴! Urgent / 30초 폴링 / ETag 304 / 5분 디바운스 / 50건·30일 cap) + Sidebar nav count badge 정착 (FU-008 흡수).
   - **시각 회귀 12 화면 제외** (사용자 ad-hoc 진행). AppShell 글로벌 top bar 신설 부정 — Sidebar `/notifications` nav item 옆 배치 (W5-D1).
   - **Phase 구조**: A (BE 단독, contract irreversible) → B (FE + spec 두 줄). 2 PR 직렬. 신규 마이그 / ADR 없음.
   - **결정 잠금 W5-D1~D10**: bell placement (Sidebar) · Urgent 표시 = 표시 시점 priority · 디바운스 = 앱 레이어 + DB 조회 (D8) · cap = lazy trim · 트리거 = `services/notifications.ts` · BE TDD 1~4, 트리거는 통합 테스트 흡수 · 두 쿼리 키 분리 · `<NavItemCountBadge>` 추상 (FU-008 흡수) · 2 PR · ADR 없음.
   - **부수 변경**: `next-session-tasks.md` Wave 5 row 추가 + 진행 순서 갱신 · `followup-bucket.md` FU-008 Resolution 에 Phase B 흡수 메모.

## 이전 세션 시작점 (2026-05-09 FU bucket P1 batch close — drift 정리 + audit closure)

→ **FU P1 batch close (docs-only, 2026-05-09)** — followup-bucket row drift 3건 + audit closure 3건.
   - **Drift 정리**: FU-001 (PR #248 `67b1eda` + harden `4fe9e25`) / FU-002 (PR #249 `264b908`) / FU-017 (PR #265 `00bfb33`) 모두 실머지 상태였으나 bucket row 가 Open 으로 남아 drift. ✅ 로 갱신 + Resolution 한 줄.
   - **Audit closure**: FU-003 (popup dismiss — 현 localStorage 구현 spec §10.3.2 정합, 코드변경 X) / FU-004 (FAQ 한국어 morpheme tokenizer — overspec, 현 substring 매치 충분) / FU-005 (urgent auto-open — spec 미정의, 정책 신설 시 별 FU). 세 건 모두 wontfix 결정.
   - **R5 위반 기록**: 본 PR 은 FU 6건 단일 묶음 PR (사용자 명시 지시 고수). docs-only 라 reversibility 영향 없음. R5 grandfather 패턴.
   - **다음 P1 잔여 = 0**. P2 batch (FU-006~010, FU-015/016/018) 진입 가능.

→ **마이그 번호** — 다음 마이그 번호 = 021 (변동 없음).

## 이전 세션 시작점 (2026-05-09 Wave 3 Phase F (W3-8) close — Wave 3 완료)

→ **Wave 3 close (W3-8 종합 검증, 2026-05-09)** — 사용자 수락 게이트 통과 시 Wave 3 완료.
   - **검증 결과**: BE 269 passed / 7 todo (26 suites) · FE 561 passed (72 files) · FE lint (`lint:tokens` 포함) 0 error · 토큰 lint (raw hex / OKLCH) 소비자 파일 0 hits.
   - **권한 매트릭스 (§8.3 / §13.2)** — per-route 분산 커버 100% 그린: `admin-{tags,trash,users,masters}.test.ts` 4-role matrix + `services/permissions/__tests__/assertCanManageVoc.test.ts` (5 회귀 + reassign + submitPayloadReview) + `vocs.test.ts` reassign 매트릭스. **단일 통합 테스트 파일은 FU-018 별 PR 후보**.
   - **이연**: FU-013/014 ✅ (PR #266/#267 Phase D+E 동봉 머지). 신규 FU-018 (perm matrix integration test) / FU-019 (equipment/db real loader) / FU-020 (multi-instance advisory lock) 등록.

→ **다음 세션 진입 후보**
   - **Wave 5 plan 작성** — Wave 3 close 후 진입 unblock. 알림 / 셸 마감 / 시각 회귀 12 화면.
   - **FU batch 처리** (P1 우선): FU-001 (dompurify XSS) · FU-002 (notice/faq 인덱스 마이그 018) · FU-003/4/5 (Wave 4 idle slot hotfix) · FU-017 (✅ 머지 완료 — 활성 row 정리).
   - **P2 batch**: FU-006~009 + FU-010 + FU-015 + FU-016 + FU-018.
   - **P3**: FU-012 / FU-019 / FU-020 (외부 의존 / NextGen).

→ **마이그 번호** — Wave 3 close 시점 다음 마이그 번호 = 021.

## 이전 세션 시작점 (2026-05-09 Wave 3 Phase D + E 머지 완료)

→ **Wave 3 Phase D + E 머지** (origin/main `8501003`, 2026-05-09): hotfix 2 + chore 1 + feature 2, 총 5 PR 단일 세션 처리.
   - **PR #266** FU-013 마이그 019 (`vocs.parent_id ON DELETE SET NULL`) — ADR 0005 §6 case 6 정합. pg-mem 회귀 3건.
   - **PR #267** FU-014 마이그 020 (`tags(name, kind) UNIQUE`) — 동일 name 다른 kind 허용 / 동일 (name, kind) 거부. pg-mem 회귀 3건.
   - **PR #268** chore: lint config (`argsIgnorePattern: '^_'`) + 죽은 `mockVocRepo` 제거.
   - **PR #269** W3-6 Phase D External Masters (`/admin/masters`) — Manager+ refresh + Manager+/Dev read, 5분 쿨다운 + atomic swap, 3-mode badge. BE 14 + FE 11 tests.
   - **PR #270** W3-7 Phase E Admin Users (`/admin/users`) — Admin only (404 hide), last-admin 409, role/is_active → `user_role_log` audit. BE 13 + FE 8 tests.

→ **에이전트 디스패치 결정 (2026-05-09)** — Phase D + E 독립이라 worktree-isolated 병렬 dispatch (`general-purpose` × 2). 머지 시 router/index 등록 줄 keep-both 충돌 1건 (resolve `be305c9`).

## 이전 세션 시작점 (2026-05-09 FU-017 Page Layout 시스템 머지 완료)

→ **FU-017 머지** (브랜치 `docs/page-layout-system`, 2026-05-09): 5 페이지 헤더 drift 제거. Spec(`uidesign.md §5` 슬롯 계약 + 액션 그룹 6규칙 + UI size 토큰) → `<PageLayout>` + `<PageHeader>` (closed surface) → Button/Input/Select 토큰화 (`--ui-h-md` 32px 통일) → VOC/Notice/FAQ/Tag Master/Trash + StubPage/HealthPage 마이그. `PageTitle.tsx` / `TopBar.tsx` 삭제. `benchmark/` 폐기 (사용자 결정 — visual-diff baseline 더 이상 참조 X). 542/542 FE 테스트 green.

## 이전 세션 시작점 (2026-05-09 Wave 3 Phase B + C 머지 완료)

→ **Wave 3 Phase B + C 머지** (origin/main `fa3986c`, 2026-05-09):
   - PR #262 (W3-4 Tag Master `/admin/tags`) merge `9959ed0` — FE+BE+fixture+test 단일 PR. 32 BE tests / 6 FE tests. Codex P1/P2 fix 동봉 (`c83245f`): PATCH/external 응답 shape (usage_count/rule_ref_count), FE 라우트 가드, 일시중지 버튼 disabled placeholder.
   - PR #263 (W3-5 Trash `/admin/vocs/trash`) merge `fa3986c` — FE+BE+fixture+test 단일 PR. 10 BE tests + 3 SQL 회귀 / 5 FE tests. Codex P0/P1 + visual P2 fix 동봉 (`5aaa46c`): voc_history 003 컬럼 정합, tag_rules.system_id 제거, openapi 403→404, --text-tertiary 토큰 정합, maxWidth wrapper 제거.
   - visual-diff baseline `benchmark/admin/01-tag-master.png` + `02-trash.png` (real screenshot baseline TBD by user).

→ **Wave 3 Phase A 머지 (참조)** — PR #250/#251/#252/#254/#253 (origin/main `f9ed85f`, 2026-05-09).

→ **다음 세션 진입: Phase D + E 병렬**
   - **Phase D** External Masters (`/admin/masters`) — OQ-2 Option B (Manager+ refresh / Manager+ + Dev read), refresh 쿨다운 5분 + atomic swap.
   - **Phase E** Users (`/admin/users`) — Admin only, last-admin guard 409, role 변경 audit `user_role_log` (마이그 017).
   - 두 phase 독립 → full-parallel 가능. 각 화면당 1 PR. 정본 plan `wave-3-admin.md §6.2 W3-6 / W3-7`.

→ **Phase F = 종합 검증** — visual-diff 4 화면 baseline 확정 / 권한 매트릭스 BE 통합 / lint / fixture-seed parity / 사용자 수락 → Wave 3 close.

→ **잔여 / 후속**
   - **FU-013** (P1, NEW) `vocs.parent_id` ON DELETE SET NULL 마이그 (003 미적용, ADR 0005 §6 case 6 정합). Phase D 진입 전 hotfix 권장.
   - **FU-014** (P1, NEW) `tags(name, kind)` UNIQUE 제약 (현재 slug UNIQUE 우연 의존).
   - **FU-015** (P2, NEW) Admin BE 테스트 service mock 의존 → DB-backed 통합 테스트.
   - **FU-016** (P2, NEW) Tag rule 일시중지 UI — 규칙 선택 진입점 (PR #262 hotfix 에서 disabled placeholder).
   - **FU-010** (P2) user_role_log CHECK constraint 보강.
   - **FU-012** (P3) Tag merge audit 도입 시 `tag_merge_log` 테이블 spec.
   - **FU-003/4/5** (P1) idle slot hotfix. **FU-006~009** (P2) Wave 3 close 후 batch.
   - **Wave 5 plan** = Wave 3 close 후 작성 진입.

→ **사고 기록 (2026-05-09)** — PR #252 cycle 중 background agent 가 stalled 라고 오판하고 force-push 한 결과 codex P1 fix 2개 (`0724825`) 가 remote 에서 덮여서 hotfix PR #254 로 복구해야 했음. 메모리 룰 등록(`feedback_no_concurrent_force_push.md`).

→ **Grill 결정 정합 (19건, 2026-05-09)** — OQ-1 Option D / OQ-2 B / OQ-3 A / OQ-4 014/015/017 / OQ-5 사이드바 B. ADR 0004/0005 Accepted.

→ **Wave 4 (공지/FAQ/Notice popup) 머지 완료** — PR #245 (08070cc, 2026-05-09).

## Active Waves

**Wave 3 — Admin 4 화면**
- ✅ Phase A 4 PR + 1 hotfix 머지 (`f9ed85f`)
- ✅ Phase B + C 머지 (`fa3986c`)
- ⏳ Phase D (External Masters) + Phase E (Users) 병렬 진입 가능
- ⏭️ Phase F 종합 검증 → Wave 3 close

**Wave 2 — Dashboard**
- ⛔ Wave 3 close 후 진입.
