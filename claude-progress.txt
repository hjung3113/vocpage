# vocpage — Session Progress

## 다음 세션 시작점

→ **Wave 1.6 C-12 VocReviewDrawer PR opened** (2026-05-03) — drawer shell parity.
   I-2 approved-lock(`review_status==='approved'` → status select disabled +
   a11y title/aria-label 사유 노출, prototype drawer-advanced.js:33 인용),
   신규 `VocReviewMetaPanel.tsx` (91 LOC, 7-field grid: status/priority/type/
   assignee/due_date/system/menu) + DialogDescription sr-only, fixtures parity
   회복(SYSTEMS/MENUS placeholder ID를 voc.fixtures.ts의 실제 UUID와 매핑).
   4-reviewer 적대적 패널(designer/code-reviewer/test-engineer/architect, 각
   3관점) → 7건 fix-required 반영 (P0 3 / P1 4) + 5건 defer(C-13 후속).
   본문/서브태스크/첨부 dropzone/Toast Editor/댓글리스트 재설계는 후속 C-13~16.
   FE 테스트 350/350 green, typecheck/lint clean. 변경 LOC: drawer 175→198,
   meta-panel 신규 97, list-page 115→128. 200줄 룰 준수.
   **다음**: 사용자 검수 → merge → C-13 VocReviewTabs (δ 종료) → ε batch 진입.

→ **Wave 1.7 Phase A MERGED** (PR #185, merge `1353219`, 2026-05-03) — VOC 등록 모달
   parity spec + master contracts. `feature-voc.md §9.11` 신설 (필드 구성·skeleton·
   본문↔첨부 단방향 동기화·합산 cap·Due Date 모달 미노출), `requirements.md §6.1`
   master endpoints (`GET /api/masters/systems` nested + `GET /api/masters/menus`),
   `shared/contracts/master/io.ts`에 `SystemListItem`/`MenuListItem` 추가,
   plan `wave-1-7-voc-create-modal.md` (Phase A→D, 결정 D1~D10).
   **Codex 적대적 리뷰 9건 → 7 반영 / 2 스킵**: BLOCKER namespace `/api/master`→
   `/api/masters`, HIGH priority 강제·due_date 자동계산 BE 미구현 → Phase B 확장,
   §8.5 합산 cap amendment, benchmark flat-index 정정, §9.4.2 `?includeArchived`
   명시. 스킵: prototype 카운터 (false positive).
   **다음**: Wave 1.6 δ batch **C-12 VocReviewDrawer 진입 승인** (2026-05-03).
   진입 게이트(γ C-11 PR #182 머지 + 사용자 검수) 충족 — 바로 착수.
   진행 순서: Wave 1.6 잔여 (C-12 → C-13 →
   ε C-14∥C-15∥C-16 → ζ C-17 → C-18∥C-19 → Phase D) → 1.7 B/C/D →
   Follow-up C-2 → Wave 2. Wave 1.7 Phase A는 spec/contract만 선행 머지된
   상태이며, B(BE FE 코드) 이후는 1.6 종료 후 진입.

→ **Wave 1.6 Phase A MERGED** (PR #126, 2026-05-02) — `/voc` prototype 분해 산출물.
   `voc-prototype-decomposition.md` ~700줄 + plan `wave-1-6-voc-parity.md`.
→ **Wave 1.6 Phase B MERGED** (PR #128, 2026-05-02) — 토큰 갭 채우기.
→ **Wave 1.6 Phase C-1 MERGED** (PR #129, merge `9b615ef`, 2026-05-02) — VocStatusBadge rebuild.
   사용자 검수에서 처리중·완료 hue 충돌 발견 → 차회에 violet 이동 결정.
→ **Wave 1.6 Phase C precedent doc MERGED** (PR #131, 2026-05-02) —
   `docs/specs/plans/wave-1-6-phase-c-precedent.md` 룰북 잠금. C1.D1~C1.D6 + per-leaf
   체크리스트 7항목 (TDD failing-first / §7.3 lint scoped / 4 reviewer + codex /
   visual-diff SKIP 0 / 사용자 시각 검수 / 구버전 동시 삭제 / FE-only slug 격리) +
   §3 N/A 절 + §5.1 신규 토큰 도입 정책 (≤3 leaf 동봉 / ≥4 Phase B addendum 분리).
   리뷰: architect/critic/code-reviewer 3종 + codex adversarial. Q3 a11y aria-label
   중복은 a11y audit follow-up으로 deferral.
→ **Wave 1.6 Phase B violet move MERGED** (PR #132, merge `395bf0f`, 2026-05-02) —
   `--status-processing-{bg,fg,border}` hue 152→290°(light)/288°(dark) +
   orphan `--status-dot-processing` align + WCAG AA 통과 (codex 직접 계산 7.72:1) +
   uidesign.md §10 동기화. TDD spot test 4 case (RED→GREEN). 사용자 시각 검수 OK.
→ **Wave 1.6 Phase C batch plan MERGED** (PR #134, merge `b013f0a`, 2026-05-02) —
   `wave-1-6-voc-parity.md` §7.2/§7.3 추가. 19 rebuild 잔여를 6 batch (α~ζ) + 1 addendum
   (B-add-1 avatar 6 토큰)로 분할. critical path 8 sequential 단계, 추정 6~10 세션.
   결정 잠금: VocAssignee 6 토큰 → addendum / AppShell → Phase D 일괄 점검 / Topbar →
   NotifButton 머지 후.

→ **Wave 1.6 Phase C-2 MERGED** (PR #136, merge `c0a7518`, 2026-05-02) — VocPriorityBadge.
   초기 구현은 prototype `--status-orange-bg/-border` 3-token chip 형태였으나, 사용자 시각 검수에서
   "영어 라벨 + 배경 없는 텍스트-only" 스타일로 디자인 변경 결정. 최종:
   - 라벨: `Urgent / High / Medium / Low` (영문). VOC 테이블 컬럼 헤더 `우선순위`는 한글 유지.
   - 색: urgent `--status-red` bold / high `--status-orange` semibold / medium `--text-tertiary`
     normal / low `--text-quaternary` normal. bg/border/padding 없음.
   - 토큰: `--status-orange` (1개)만 도입, `--status-orange-bg/-border`는 본 PR에서 제거.
   - aria-label: `Priority ${label}` (영문).
   - 4 reviewer (architect/code-reviewer/test-engineer/designer) + codex adversarial 통과.
     Codex 2 medium 발견 (visual-diff selector `.d-badge-*`→`.pri-badge.p-*` / §5↔CSS drift) 둘 다 적용.
     Designer가 초기 chip 격상 FAIL 지적 → 결국 사용자 결정으로 text-only 회귀하며 spec drift 자연 해소.
   - 정본 spec: `uidesign.md §5 Priority Badge` + `§10 --status-orange` + screenshot
     `docs/screenshots/wave-1-6/phase-c-2-priority-badge.png`.

→ **Wave 1.6 Phase C-2.5 MERGED** (PR #138, merge `a8e3507`, 2026-05-02) — VOC badge audit + 3-archetype lock.
   `docs/specs/reviews/wave-1-6-voc-badge-audit.md` (~457줄) + `uidesign.md §13 Badge System` 신규 +
   기존 §13 (Admin·Notice·FAQ) → §14 재번호 + 12 sub-section 갱신 + 5 active doc cross-ref 갱신 +
   10 archive doc banner. 잠금 결정:
   - 3 primitive archetype: TextMark / OutlineChip / SolidChip (모두 pill radius, shipped 일치)
   - 4 semantic wrapper: VocTypeBadge(신설) / VocTagPill(C-3) / VocStatusBadge·VocPriorityBadge(C-2.6 refactor)
   - VocTypeBadge dynamic contract `{ slug, name, color? }` — admin 추가 타입도 `Tag` fallback 렌더
   - 7 chip 토큰 (`--chip-{height-sm,padding-x-sm,radius-pill,radius-rounded,font-size-sm,gap,dot-size}`)
     B-add-2로 분리, `tokens.ts` SSOT 우선 + `index.css` 미러 + grep 일치 검증
   - presentational vs interactive 하드 분리 — FilterChip/SortControl 별개 `<button>` 컴포넌트
   - status enum 5값 확정 (`received|reviewing|processing|done|drop`), 6값 모순 수정
   - C-2.6 self-sufficient — TDD RED spec(data-testid + DOM/aria) + migration delta 포함
   리뷰: 내부 3 round (architect/critic/verifier 모두 SHIP) + Codex adversarial (high+medium 2건 모두 적용)
   + refactoring review (5 체크리스트 / radius 모순 적용). 4 commits + 1 reviewer-fix commit.

→ **Wave 1.6 B-add-2 MERGED** (PR #140, 2026-05-02) — chip token SSOT.
   `frontend/src/tokens.ts` 7 chip 토큰 추가 (`chipHeightSm/PaddingXSm/RadiusPill/RadiusRounded/FontSizeSm/Gap/DotSize`)
   + `frontend/src/styles/index.css :root` 동등 미러 (`--chip-height-sm` … `--chip-dot-size`) +
   `.status-badge` 유일 raw `9999px` literal → `var(--chip-radius-pill)` 마이그.
   값: 20px / 8px / 9999px / 4px / 11.5px / 4px / 6px (audit §7 spec 일치). Filter Tab raw literal은
   `uidesign.md` spec text 안에만 존재 → 본 PR scope 외, C-2.6/C-3에서 자연 흡수.
   Acceptance: grep parity 7↔7 / 204 FE tests green / production build clean / 5 CI checks pass /
   Codex adversarial review **ship — zero findings**. 시각 검수: `/voc` 5 status badge pill radius
   유지 확인 (screenshot `docs/screenshots/wave-1-6/b-add-2-chip-tokens-voc-list.png`).
   `chipRadiusRounded` (4px)는 reserved — v1 caller 0개, audit §4.1 명시 deferred to CountPill.

→ **Wave 1.6 Phase C-2.6 MERGED** (PR #142, merge `8115741`, 2026-05-02) — primitive 3종 + VocTypeBadge 신설 + wrapper refactor.
   `frontend/src/shared/ui/badge/{TextMark,OutlineChip,SolidChip}.tsx` 신규 (TDD RED→GREEN, span-only,
   no role, dimension은 `--chip-*` 토큰으로 inline-style 매핑) + `voc-type-icon.ts` slug→{Icon,color,weight}
   + fallback (`Tag`/`--text-tertiary`) + `VocTypeBadge` (단일 prop shape `{slug,name,color?}`, color v1 ignore,
   unknown은 audit §3 mandate대로 `data-testid="text-mark-unknown"`) + VocStatusBadge / VocPriorityBadge
   내부 위임 refactor (public API + outer testid + aria-label 보존). `.status-badge*` / `.priority-badge*` /
   `.s-*` / `.p-*` / `.status-dot` / `.priority-icon` ~85 LOC index.css 동일 PR 삭제. 신규 토큰 0개
   (B-add-2 chip 토큰 재사용). 모든 파일 ≤45 LOC (≤200 한도).
   - 사용자 시각 검수 결정: **High 우선순위 bold 제거** — `weight 600 → 400` (Urgent 700만 강조 유지) +
     audit §3.2 / §2.1 priority 표 동기화.
   - 검증: 249 vitest pass / tsc / build / lint:tokens / 0 hex 모두 그린.
   - Codex adversarial review: BLOCKER 1건 (unknown testid) + MEDIUM 1건 (구조 단언 부재) 둘 다 적용,
     HIGH 1건 (chip 토큰 4px/11.5px vs 레거시 5px/11px) audit §2.3 의도된 통합으로 거부 — 시각 검수
     스크린샷이 parity 증명. 자체 재리뷰 후 적용 여부 결정.
   - 정본 spec: `docs/specs/reviews/wave-1-6-voc-badge-audit.md` §2/§3/§7 + screenshot
     `docs/screenshots/wave-1-6/c-2-6-primitives-voc-list.png`.

→ **Wave 1.6 Phase C-3 + C-4 MERGED** (PR #144, merge `d2f0762`, 2026-05-02) — VocTagPill + VocSubRow + TextMark 확장.
   `frontend/src/components/voc/VocTagPill.tsx` (TextMark wrapper, size='xs', '#' glyph) +
   `frontend/src/features/voc/components/VocSubRow.tsx` (VocStatusBadge + trailing slot — 기존 wrapper만,
   새 badge 0개). 신규 토큰 1개 (`--chip-font-size-xs: 10.5px` — audit §5.1 ≤3 leaf 동봉 OK).
   - **C-3 디자인 피벗**: 초기 구현은 audit §1 락대로 OutlineChip wrapper(brand-bg + border + 9999px).
     사용자 시각 검수에서 "테두리/배경 없는 회색 글씨로 더 작게" → TextMark `size='xs'` (10.5px,
     `--text-quaternary`, weight 500, '#' prefix)로 재작성. **TextMark primitive API 확장**:
     `icon: LucideIcon | '#'` + `size?: 'sm' | 'xs'` (default 'sm', backward compatible). **신규 4번째
     archetype 도입 없음** — audit §2.4 closure 유지. 기존 wrapper(VocType/Status/Priority) regression 0.
   - **C-4**: VocSubRow는 prototype `sub-row` div를 React `<button>`으로 재현. props
     `{ status, title, onClick?, trailing? }`. trailing slot은 C-5 VocAssignee 진입 시 채워질 자리.
     production wiring (VocReviewDrawer 통합)은 audit §7 범위 외 — C-7 VocRow 또는 Phase D에서 흡수.
   - **검증**: 263 vitest pass / build clean / no hex|OKLCH / lint 그린 / 4 commits + 4 documents synced.
   - **리뷰**: codex adversarial (SHIP, 1 medium + 3 low → 3건 적용 / 1건 사유 명시 거부) +
     Sonnet 3-expert review (20/30 FIX → BLOCKER 1 spec sync + HIGH 1 TextMark 직접 테스트 +
     MEDIUM 1 xs height 주석 + LOW 1 weight prop 모두 적용) + Sonnet verifier 재검증
     (stale cross-ref 1건 발견 → c037571에서 해결).
   - **AMENDED 정책 도입**: 잠긴 spec line 변경 시 strikethrough + 원문 인용 + 사유 + 날짜 footnote
     (`[AMENDED 2026-05-02 post-PR-144 visual review]`) 적용. 향후 wrapper 디자인 피벗 시 동일 패턴 사용.
   - **정본 spec**: `wave-1-6-voc-badge-audit.md` §1 row 2 / §2.1 / §2.2 / §2.4 / §4.1 / §4.2 / §7 +
     `uidesign.md` §5 / §10 / §13 + `wave-1-6-voc-parity.md` §7.2 leaf 모두 동기화.
     screenshots: `docs/screenshots/wave-1-6/c-3-tag-pill-v2-textmark-xs.png` (PR comment 첨부).

→ **Wave 1.6 Phase C-5 MERGED** (PR #146, merge `4feee35`, 2026-05-02) — VocAssignee + VocTable wire-up.
   - VocAssignee.tsx (78L) — `{ name: string|null, colorClass?: 'steel'|'teal'|'violet' }`. 미배정 시 `UserX + 미배정`,
     배정 시 22px 원형 이니셜 + 이름 (text-tertiary 11.5px). `hashAssigneeColor(name) % 3 → steel|teal|violet`
     (deterministic). `colorClass` prop으로 caller override 가능 (mock data layer / parity testing 대비).
   - **B-add-1 폐기**: 잠긴 plan §7.2는 "avatar colorCls 6 토큰 → §5.1 ≥4 → addendum 별도 PR" 강제했으나,
     decomposition.md:511 "6종" 표기가 prototype 실제(`list.css:L289–L303` default+.g+.p = 3종, `helpers.js:L19`
     `assigneeCls` 단일 string)와 어긋난 오기였음. 실 토큰 3개(`--avatar-steel/-teal/-violet`)는 Phase B PR #128
     시점 이미 `index.css:153–155` 추가 완료. §5.1 ≤3 leaf 정책상 inline 동봉 가능 → addendum PR 불필요.
     `decomposition.md:511` + `parity.md §7.2 α-batch / §7.3 critical path / PR 카운트 22→21` 모두 AMENDED 동기화.
   - VocTable.tsx 인라인 plain-text 렌더 → `<VocAssignee />` 교체 (precedent §C1.D8 구버전 동시 삭제).
   - 11/11 unit (RED→GREEN) + VocTable 7/7 + integration 5/5. Build green, full FE suite 286 PASS.
   - Codex adversarial 5건 처리: P1 wire (적용) / P2 hash 정확 매핑 (architectural prop override + C-7로 이관) /
     P2 test 강화 (적용) / P2 visual-diff snapshot (Phase D로 deferral, C-1~C-4 선례 일치) / P3 empty/whitespace (적용).
   - 9-항목 인하우스 패널 SHIP 9/9. 사용자 검수 대체 = live /voc 통합 캡처 PNG.
   - Screenshots: `docs/specs/reviews/c-5-voc-assignee.png` (데모 HTML) + `c-5-voc-assignee-live.png` (실 통합).

→ **Wave 1.6 Phase C-6 MERGED** (PR #148, merge `9f5004e`, 2026-05-02) — VocListHeader (standalone).
   - VocListHeader.tsx (129L) + test (125L) + index.css scoped CSS class. 7-cell sticky grid
     (`22px 144px 1fr 115px 108px 84px 96px`), prototype `.list-header` (list.css:1-60) 포팅.
   - sortable: `issue_code / status / priority / created_at` (4종, `<button role="columnheader">`).
     non-sortable: expand placeholder (`role="presentation"`, accessible name 회피) / 제목 / 담당자.
   - Sort affordance: ChevronUp(asc) / ChevronDown(desc) 11×11, opacity 0.35 → 1, sortFlip @keyframes
     (`scaleY(0.4)→1`, 0.22s cubic-bezier(0.16,1,0.3,1)). `key={sortDir}` 으로 방향 변경 시 remount 재생.
   - A11y: `role="row"` + cell `role="columnheader"` + `aria-sort` (asc/desc/none) + `aria-label="정렬: {label}"`
     (방향은 aria-sort에 위임, 중복 announce 회피). JSDoc에 parent must provide `role="grid"` 명시 (C-7 책임).
   - 토큰 정책: 신규 design token 0. CSS scoped class `.voc-list-header-{container,hcell,icon}` +
     `@keyframes vocListHeaderSortFlip` 만 `index.css` 추가 (transition / :hover / keyframes —
     인라인으로 표현 불가). container box-shadow는 prototype 2-layer (`light-dark` wrapped color-space) 포팅.
   - 5-reviewer 병렬 적대적 리뷰 + 7건 fix 적용:
     - architect (opus): A3 2-layer shadow + B1 focus-visible + C1 inline+class 중복 해소
     - code-reviewer (opus): B2 expand cell empty columnheader → role=presentation + C2 token-purity 테스트가
       index.css C-6 마커 블록도 검사
     - designer (sonnet): A3 / B1 / C3 expand padding-right 누수 0
     - security (sonnet): SHIP (0 findings)
     - test-engineer (sonnet): B1 expand 빈 라벨 검증 추가; B2 `sortBy='title'` non-sortable은
       `VocSortColumn` enum (`['created_at','updated_at','priority','status','due_date','issue_code']`) 미포함으로
       TS-prevented; A1 TDD 단일 커밋 process note acknowledged.
   - 12/12 vitest GREEN, build clean. lint:tokens는 코멘트 내 `oklch(` 부분문자열도 차단 → 코멘트
     "light-dark wrapped color-space literals" 로 rephrase 필요 (별도 커밋 4c771ec).
   - VocTable.tsx 미수정 — 통합은 C-7에서.
   - Screenshots: `docs/screenshots/wave-1-6/phase-c-6-list-header.png` (initial preview) +
     `phase-c-6-list-header-final.png` (post-adversarial).

→ **Wave 1.6 Phase C-7 MERGED** (PR #150, merge `c97f19c`, 2026-05-02) — VocRow + VocTable grid 재합성.
   `frontend/src/components/voc/VocRow.tsx` 신설 (76줄, 52px height, prototype `.voc-row` 포팅,
   hover/selected 상태, 키보드 a11y Enter/Space, 7-cell grid `22px 144px 1fr 115px 108px 84px 96px`).
   VocTable.tsx 재합성 — `<DataTable>` 제거 → `<div role="grid" aria-label="VOC 목록"><VocListHeader/><VocRow[]/></div>`.
   Phase B PR #128 토큰 (`--voc-row-hover-bg` / `--voc-row-selected-bg`) 재사용 — 신규 토큰 0.
   `=== C-7 VocRow styles ===` token-only marker block (token-purity scan 추가) +
   `vocGridLayout.ts` 신설 (VOC_GRID_COLUMNS / VOC_GRID_PADDING_X 공유 상수 — VocRow ↔ VocListHeader 중복 제거).
   5 reviewer adversarial: architect FIX_REQUIRED (empty-state in role=grid + grid-cols 중복 + inline style)
   + designer FIX_REQUIRED (token 확인 + title gap + expand hover) + code-reviewer/security/test-engineer SHIP.
   적용된 fix 3건 — empty-state를 role=grid 외부로 이동 + `.voc-table-empty` 클래스화 +
   `gridTemplateColumns` 공유 상수화 + role=grid/empty-state 명시 test 2개 추가.
   Deferral: expand-btn hover transform (expand 인터랙션 미구현 — subtask wave) /
   title gap (inline 형제 없음 — YAGNI). 303 FE + 92 BE tests green. 스크린샷 `wave-1-6-c-7-vocrow.png`.
   C-6 deferral 자동 흡수: visual-diff harness (parent role=grid 환경에서 측정 가능).
→ **[CORRECTION 2026-05-02]** 직전 기재 오류 — "Phase C complete / 다음 = Phase D"는 사실
   plan §7.2의 **α-batch (C-1~C-7)만 완료**. β/γ/δ/ε/ζ **12 PR이 통째로 미착수**.
   미진행: β(C-8 SortChips ∥ C-9 AdvancedFilters ∥ C-10 NotifButton+Panel) / γ(C-11 CreateModal+AttachmentZone) /
   δ(C-12 ReviewDrawer → C-13 ReviewTabs) / ε(C-14 CommentList ∥ C-15 InternalNotes ∥ C-16 SubTaskList) /
   ζ(C-17 SidebarUserSwitcher → C-18 Sidebar ∥ C-19 Topbar). Phase D는 §8 정의상 종합 검증이라 모든
   batch 머지 후에만 진입 가능 — 직진 불가. 사용자 시각 검수에서 navbar/레이아웃 깨진 것으로 확인됨
   (ζ 미진행이 원인).

→ **Wave 1.6 Phase C-8 MERGED** (PR #153, merge `57b56e8`, 2026-05-02) — VocSortChips rebuild.
   Tailwind+inline style → prototype 클래스(`.list-toolbar` / `.sort-label` / `.sort-chip[--active]` /
   `.sort-chip-icon`) 마이그. CSS는 `index.css` `=== C-8 VocSortChips styles ===` marker block.
   신규 토큰 0. props 시그니처(`sortBy/sortDir/onChange`) 불변. a11y `radiogroup aria-label="정렬"` +
   icon `aria-hidden`. TDD RED `553c63f` → GREEN `83c5738`. 8/8 컴포넌트 + 306/306 FE 전체 PASS.
   Codex 1차 review clean. Codex `/codex:adversarial-review` 2차 HIGH 1건 — 정렬 컬럼셋이 spec
   line 345 (`issue_code|title|status|assignee|priority|created_at`)와 drift (현 enum:
   `created_at|updated_at|priority|status|due_date|issue_code`). C-8 visual rebuild 스코프 외로
   판정, follow-up 분리. 사용자 시각 검수에서 VocRow에 `VocTagPill` 통합 누락 추가 발견 (C-7 gap).
   Screenshot `docs/specs/screenshots/wave-1-6-c-8-voc-sort-chips.png`.

→ **Issue #156 MERGED** (PR #159, merge `c9a8768`, 2026-05-02) — VocRow tag-row 통합.
   `VocRow.tsx`에 `<div class="tag-row"><VocTagPill .../></div>` 추가 + `.tag-row` CSS
   (`index.css` C-7 marker block). TDD RED→GREEN. Codex adversarial 1 round 적용.
   C-7 사용자 시각 검수 누락분 보정. 스크린샷 PR #159 코멘트 첨부.

→ **Issue #155 MERGED** (PR #161, merge `e579475`, 2026-05-02) — VocSortColumn spec align.
   `shared/contracts/voc.ts` `VocSortColumn` enum 교체 — 제거 `updated_at`/`due_date`,
   추가 `title`/`assignee`, 유지 `created_at`/`priority`/`status`/`issue_code`. BE `/api/vocs?sort=`
   핸들러에 `title`/`assignee` SQL 정렬 추가. FE URL state 마이그레이션 fallback (구 query string
   안전 처리). Codex adversarial 1 round 적용. 스크린샷 `docs/specs/screenshots/wave-1-6-c-8-voc-sort-chips.png`
   (Issue 155 컨텍스트). C-8 시점 follow-up drift 해소 완료.

→ **opt-prompt skill MERGED** (PR #163, merge `dd6a947`, 2026-05-03) — 자동조종 prompt right-sizer.
   `.claude/skills/opt-prompt/` (~268L) — 단일 task 평가 후 워크플로우 복잡도 자동 조정 + eval 모드
   self-improvement 피드백 루프. `/opt-prompt` 명시 호출만 트리거 (자동 키워드 인식 차단).

→ **Issue #162 MERGED** (PR #165, merge `eba12c8`, 2026-05-03) — sticky 헤더 갭 수정.
   `VocListHeader` `position: sticky; top: 0` 가 `<main>`의 `p-6` (24px) padding-top 안쪽 edge에
   걸려서 그 위 24px 영역으로 행이 비쳐 보이던 버그. `top: calc(0px - var(--sp-5))` 로 sticky 정지점을
   24px 위로 끌어올려 opaque 헤더 배경이 패딩 갭을 덮도록 수정 (코드 변경 4줄). before/after 스크린샷
   `docs/specs/screenshots/issue-162/{before,after}.png` 첨부. `/opt-prompt` small 사이즈 적용 →
   직접 Serena+rg → PR → codex adversarial 시도 (CLI hang) → 사용자 승인 후 수동 self-review →
   merge. Follow-up: **#166** (`--sp-5` ↔ AppShell `p-6` silent coupling 을 시맨틱 토큰으로 정리 +
   visual-regression 가드 — 다음 wave 이월).

→ **Issue #166 MERGED** (PR #169, merge `e2c2cd6`, 2026-05-03) — sticky 헤더 ↔ AppShell padding 토큰화.
   `--app-main-pad` 시맨틱 토큰을 `frontend/src/styles/index.css` SSOT 으로 신설하여
   `AppShell <main>` 의 `padding` 과 `VocListHeader` 의 sticky `top` compensation 두 consumer 가
   동일 토큰을 참조하도록 정리. 이전 PR #165 의 `--sp-5` silent coupling 제거. 회귀 가드:
   `VocListHeader.test.tsx` 가 sticky `top` 의 `--app-main-pad` 참조를 강제하고,
   신설 `frontend/src/components/layout/__tests__/AppShell.tokens.test.ts` (3 assertion) 가
   `<main>` 측 결합도 정적 검사 (Tailwind `p-N` / raw `--sp-*` 금지). `/opt-prompt` small 사이즈 →
   직접 Serena+rg → PR → code-reviewer agent 1× → P2 (AppShell coupling test) + P3 (token rename
   `--app-main-pad-y` → `--app-main-pad`) 적용 → merge. Out-of-scope: `vocGridLayout.ts` 의
   `--sp-5` consumer (별도 follow-up 가능).

→ **Wave 1.6 Phase C-9 MERGED** (PR #172, merge `8b6c195`, 2026-05-03) — VocAdvancedFilters rebuild.
   In-place rewrite — inline Tailwind 색상 유틸 + inline `style={{}}` 을 `frontend/src/styles/index.css`
   `.advanced-filters*` semantic class block 으로 이전. C-8 dash-style 네이밍 컨벤션 미러링.
   prop contract 무변경 (callsite `VocListPage.tsx:42-56` 그대로). 169→156 LOC.
   Open/close 애니메이션은 uidesign §9 prescribed pattern 준수: `grid-template-rows: 0fr → 1fr` +
   easing `cubic-bezier(0.16, 1, 0.3, 1)` (이전 안의 `max-height` 시안은 4-expert 리뷰의
   design-system blocker 로 잡혀 fixup 적용). Active chip fill 도 fixup 으로 C-8 `.sort-chip--active`
   미러 (`--brand-bg` + `--accent` outline). 패널은 항상 DOM 에 존재하고 `aria-hidden` 이 open/close
   신호 — adversarial review 의 visual-harness 우려는 harness 가 attribute 를 읽는 방식으로 흡수.
   Tests 5→6 (behavioral; class-assertion tautology 2개 → aria-hidden flip guard 1개로 통합).
   324/324 FE pass, typecheck clean. 적대적 리뷰 4인 (architect/test/designer/critic) 패널 통과.
   /opt-prompt decision id `opt-20260502T194032Z-c-9-voc-advanced-filters` (retro 는 헬퍼 로그
   `$HOME` 이전 전 손실로 미기재).

→ **Post-merge 사용자 피드백 (2026-05-03, C-9 직후)** — `/voc` 시각 검수에서 3건 발견. 구현 보류, 기록만:
   - **F-1 정렬 칩↔헤더 정렬 불일치**: 칩 6개 vs 헤더 4개(title/assignee 누락). 사용자 결정 = **Option A
     (칩 제거 + 헤더 6개 모두 정렬 가능)**. C-8 산출물 일부 롤백.
   - **F-2 제목 좌측 voc_type 뱃지 누락**: `VocRow.tsx:55-66` 가 `<VocTypeBadge>` 미호출. contract 변경 불필요
     (`VocListItem.voc_type_id` + master `vocTypes` 이미 가용 — `vocTypeMap` prop drill 만 추가).
   - **F-3 필터 더보기 토글 위치 + 검색 placeholder**: prototype 은 status chips 행 **우측 끝**에 토글
     배치 (한 줄). 현재 React 는 별도 줄. 또 `VocTopbar.tsx:46` placeholder `"검색..."` → prototype
     `"제목, 본문 검색..."` 1줄 수정 동반.
   상세는 `docs/specs/plans/wave-1-6-voc-parity.md §12`. 처리 시점 추천: **Phase D 진입 전 단일
   follow-up PR (F-1/F-2/F-3 묶음)** 또는 Phase D 흡수 — 사용자 결정 대기. C-10 진입은 막지 않음.

→ **F-1/F-2/F-3 묶음 PR MERGED** (PR #175, 2026-05-03) — 사용자 시각 검수 후 소규모 묶음 PR.
   - F-1: VocListHeader CELLS 에 `sortKey: 'title'/'assignee'` 추가 + **VocSortChips 전면 제거**
     (component + test + C-8 CSS block + VocListPage callsite). 통합 테스트는 sort chip click →
     priority header click 으로 마이그레이션. 헤더가 유일한 정렬 affordance.
   - F-2: VocRow 에 `vocTypeMap?: Record<string, {slug, name}>` prop drill — VocListPage 에서
     `ctrl.masters.vocTypes` 로 useMemo 빌드 → VocTable → VocRow → `<VocTypeBadge>` 렌더.
     contract 변경 0. `voc-row-title` 에 `gap: var(--sp-2)` (badge↔title 여백).
   - F-3: VocAdvancedFilters 를 panel-only 로 분리 + 새 `VocAdvancedFiltersToggle` export +
     VocStatusFilters 에 `rightSlot` prop 추가 → 토글이 status chips 와 같은 row 우측 정렬.
     reset 버튼은 panel header 로 이동. VocTopbar placeholder `"검색..."` → `"제목, 본문 검색..."`.
   /opt-prompt decision id `opt-20260503T031833Z-wave-1-6-f123-bundle-v2` (retro `correct`).
   1× codex adversarial review 가 1 major (closed panel 의 chip buttons 가 `aria-hidden` 인데도
   탭 가능 → focus trap) 발견 → 패널 내용을 `open` 조건부 렌더로 변경 + 회귀 테스트 추가 (commit 64db108).
   사용자 후속 피드백 4건 반영 (commit 8366947): F-1 sort-chip 제거 누락분 보강, type↔title 여백,
   PR description 의 type vs tag 뱃지 표기 정정, fixture 한계 (모든 row 동일 voc_type_id) 답변.
   **Deferred minor 3건 (다음 세션 follow-up)**:
   - VocAdvancedFiltersToggle `aria-expanded` 에 `aria-controls` 매칭 (panel id 부여)
   - VocListPage `vocTypeMap` 빌드 → VocTable → VocRow 통합 테스트 (현재 unit 만 커버)
   - VocStatusFilters `rightSlot` 렌더 path 단위 테스트
   최종: 322/322 vitest green, tsc clean. 스크린샷 `docs/specs/reviews/wave-1-6/screenshots/post-c9-f123-bundle.png`.

→ **F-bundle deferred minor 3건 cleanup PR MERGED** (PR #178, 2026-05-03) — 56 LOC FE-only, 1 commit.
   - VocAdvancedFiltersToggle: 신규 export `VOC_ADVANCED_FILTERS_PANEL_ID = 'voc-advanced-filters-panel'` 상수 +
     toggle `aria-controls={...}` + panel `<div id={...}>`. a11y wiring 직접 매칭 테스트 추가.
   - VocListPage → VocTable → VocRow 통합 테스트: `aria-label="유형 기능 요청"` 카운트로 vocTypeMap end-to-end
     스레딩 검증. VOC fixtures 의 `voc_type_id` 가 모두 TYPE_PRIMARY (slug `feature`, name `기능 요청`) 라
     단일 aria-label 매칭으로 row 카운트 비교 가능. 단, slug `feature` 는 `getVocTypeIconConfig` known
     slug list (`bug`, `feature-request`, `improvement`, `inquiry`) 에 없어 `isUnknown=true` → testid
     `text-mark-unknown`. aria-label 은 name 기반이라 영향 없음. testid 검증은 의도적으로 스킵.
   - VocStatusFilters `rightSlot` 2 테스트: with rightSlot → 7 buttons (6 pills + 1), without → 6 buttons + `.ml-auto` 0개.
   /opt-prompt decision id `opt-20260503T045414Z-f-bundle-minor-cleanup` (retro `oversized` —
   screenshot gate 가 자동 추가됐으나 a11y attribute + tests-only 변경이라 미적용. opt-prompt
   Expand list "UI change → screenshot" 조건을 "DOM/style change visible to end user" 로
   좁힐 후속 issue 후보).
   1× codex adversarial review clean (no findings). 326/326 vitest green, tsc clean. 스크린샷
   미첨부 (변경이 ARIA attribute + 테스트 추가뿐, 가시 변화 0).
   토큰 사용량 (cost metric `input + cache_creation`): **1.16M** (PR #175 baseline 3.39M 의 ~34%).

→ **Wave 1.6 C-10 NotifButton + NotifPanel MERGED** (PR #180, merge `a8b533b`, 2026-05-03 14:46 KST) —
   FE-only, 9 files, +324/-207. β-batch 종료, ζ batch C-19 Topbar prerequisite 해제.
   - **Split**: 기존 monolithic `NotificationBell` (DropdownMenu 기반) → `NotifButton` (Popover trigger, 50 LOC) +
     `NotifPanel` (Popover content + chip filter row + item list, 131 LOC). `VocNotificationsDropdown` 은
     `Popover` + 두 자식 합성 wrapper로 재작성. orphaned `NotificationBell.tsx` + 테스트 동시 삭제 (단일
     caller `VocTopbar` 마이그레이션 완료, no back-compat shim).
   - **A11y wiring (F-3 precedent 답습)**: `NOTIF_PANEL_ID = 'notif-panel'` 상수 export → `NotifButton`
     `aria-haspopup="dialog"` + `aria-controls={NOTIF_PANEL_ID}`, `NotifPanel` `role="dialog"` + `id={NOTIF_PANEL_ID}`.
     Escape close + focus return verified. chip toggle group `role="group"` + `aria-pressed` 단일선택.
   - **Type filter chips**: prototype 7개 VOC-카테고리 칩 (문의/환불/장애/요청/배송/FAQ) **미사용** —
     실제 `NotificationType` enum (`mention | assigned | status_change | comment`) 4종과 불일치. prototype-as-reference
     룰 적용: UX 패턴 보존, content 는 실제 contract 에 적응. **No contract change.** 칩 5개 (전체 + 4 enum) 는
     `NotificationType.options` 로부터 derive (compile-time drift safety — schema 추가 시 TS 에러).
   - **Tests**: 4 → 12 (a11y wiring, badge "99+" cap, Escape close, 5 chip render, "전체" default active,
     filter behavior, empty state, item click, "모두 읽음" hidden when unreadCount=0, `data-unread` attr).
   - **Review**: 1× code-reviewer subagent adversarial 3-hat (a11y / React / token-discipline) — verdict **SHIP**.
     2 NIT 적용 후 commit: (1) `aria-haspopup="dialog"` on NotifButton, (2) `TYPE_CHIPS` derive from enum.
     No BLOCKers. 4-reviewer panel 은 사용자 simplification 권한 ("복잡하지 않다면 워크플로우 간소화 가능") 으로
     1× 로 축소. Watchdog 미사용 (main session 동기 orchestrator).
   - **Verification**: 329/329 vitest green, tsc clean. 스크린샷 `docs/screenshots/wave-1-6/c-10-notif-panel.png`
     (panel open + 5 chips + "전체" active + unread accent dots + "모두 읽음" link visible).
   /opt-prompt decision id `opt-20260503T053143Z-c10-notif-button-panel`. retro = `expanded` (사이즈는 small
   으로 시작했으나 discovery 후 medium 으로 확대 — 기존 NotificationBell 골격이 misleading 했음). rework rounds 0.
   토큰 사용량은 retro JSONL 자동 기록.

→ **Wave 1.6 C-11 VocCreateModal + AttachmentZone MERGED** (PR #182, merge `3e62bc7`, 2026-05-03 16:13 KST) —
   FE-only, 6 files, +350/-10. γ-batch 1/1 완료, δ batch C-12 VocReviewDrawer prerequisite 해제.
   - **New atom**: `frontend/src/shared/ui/AttachmentZone/AttachmentZone.tsx` (193 LOC) — image-only
     (png/jpeg/gif/webp), 최대 5개, 파일당 10MB, drag&drop + click-to-pick, "0/5" count badge,
     per-file remove. 검증 실패 시 `role=status aria-live=polite` 영역에 한국어 에러 노출. 상수 모듈 export
     (`ATTACHMENT_MAX_FILES / _MAX_SIZE_MB / _ACCEPT`) — 후속 backend 인입 시 props 주입으로 1줄 swap.
   - **Modal slot**: `VocCreateModal` 본문 에디터 아래 `<AttachmentZone files onChange disabled />` 슬롯
     1줄 주입. `File[]` state owned by modal, `open` toggle 시 reset. `onSubmit` 시그니처 `(payload, files)`
     로 확장 (호환성 깨는 변경, 단일 caller `useVocPageController.create.onSubmit` 동기 마이그레이션 +
     TODO 주석으로 추후 BE upload endpoint wiring 표시 — 본 PR 은 FE-only γ batch 정책 답습).
   - **A11y wiring**: dropzone `role=group aria-labelledby aria-describedby`, 단일 `<label htmlFor>` (이전
     초안의 `<span><label></span>` 중첩 무효 HTML 수정), file remove `aria-label="<filename> 제거"`,
     count badge `aria-label`. role=alert (assertive) → role=status (polite) — input validation 적정 패턴.
   - **Token discipline**: 에러 색상 `--status-drop` (orange "Drop" status badge token, 의미 불일치) →
     `--danger`. 신규 hex/oklch 0건. 기존 modal 의 title-error 같은 misuse 는 surgical-changes 룰로 본 PR
     scope 외 — F-bundle 후속 따로 정리 가능.
   - **Layout 후속 fix (commit `d257274`)**: 사용자 검수에서 1920px viewport 모달이 자식 overflow 발견 —
     DialogContent 의 `grid` 트랙이 자식 min-content (Toast UI Editor `previewStyle=vertical` + 3-col
     grid) 초과 시 강제 확장하는 CSS grid 거동. fix: DialogContent `w-[min(92vw,42rem)]` + `overflow-x-
     hidden` + `max-h-[90vh]` + `overflow-y-auto`, form/grid columns/본문 wrapper 모두 `min-w-0`. Pre-
     existing bug 였으나 같은 모달이라 동봉. vitest modal 7/7 GREEN 유지.
   - **Tests**: AttachmentZone 9 (constants / count badge / add via input / size reject / type reject
     (fireEvent bypass jsdom accept filter) / count exceed / remove / error clears on remove / label
     associations) + VocCreateModal 7 (기존 4 + slot rendered + empty files in onSubmit + files forwarded).
     full vitest 341/341 GREEN, tsc clean.
   - **Review**: 2× parallel adversarial (사용자 simplification 4→2). Reviewer 1 `code-reviewer` SHIP
     (NIT only). Reviewer 2 `general-purpose` (a11y/token/UX) BLOCK×3 (invalid HTML / missing aria /
     wrong token) → 모두 fix commit `f368fde` 반영. NIT 부분 채택 (key collision via WeakMap, error-
     clear-on-remove, min-w-0 truncation). 미채택: partial-batch acceptance, modal reopen reset test
     — 본 PR 는 atomic-batch 의도 + reset 로직은 useEffect 단순체.
   - **Verification**: 341/341 vitest, tsc clean, ESLint clean, token grep 0 hits, 1920×1200 Playwright
     screenshot `docs/screenshots/wave-1-6/c-11-voc-create-modal.png` (modal 완전 contained, AttachmentZone
     dropzone + 0/5 badge + "이미지 드래그 또는 선택" + "파일당 최대 10MB" 가시).
   /opt-prompt decision id `opt-20260503T063921Z-c-11-voc-create-modal`. Outcome: as-planned (NIT loop 1
   round). Layout fix 는 scope-creep 아님 — 동일 modal 시각 검수 결과.

→ **다음 세션 첫 작업** = **δ-batch C-12 VocReviewDrawer 진입**.
   §7.2 batch 순서: ~~α (C-2~C-7)~~ → ~~β (C-8~C-10)~~ → ~~γ (C-11)~~ → **δ (C-12 → C-13)** → ε → ζ → Phase D.
   - C-12 scope: VocReviewDrawer rebuild (drawer shell). C-13 VocReviewTabs sequential dependent.
   - 진입 게이트: 본 PR (#182 C-11) 머지 + 사용자 검수 통과 — **충족**.
   참고: ζ batch C-19 Topbar 는 NotifButton 머지 (C-10) 이후 unblock 상태. ε 와 병렬 가능 시점 확인 필요.
   - C-7 scope: `VocRow.tsx` 신설 (52px height, prototype `.voc-row` 포팅, hover/selected 상태,
     22px expand-btn + 6 cell). VocTable.tsx 재합성 — `<DataTable>` → `<VocListHeader> + <VocRow[]>`.
     parent에 `role="grid"` 부여하여 C-6의 a11y contract 충족 (column/row chain 완성).
   - Phase B PR #128 시점 이미 추가된 `--voc-row-hover-bg` (`index.css:149`) + `--voc-row-selected-bg` (line 150) 사용.
     신규 토큰 0 유지. expand-btn 자체는 prototype `.expand-btn` (list.css:114-128) 포팅 — 별도 컴포넌트로 빼지 않고 inline.
   - VocSubRow는 C-4에서 이미 있음 — VocRow가 자식으로 호출하는 합성 패턴.
   - C-6 deferral 항목 자동 흡수: visual-diff harness PASS line (parent role=grid 환경에서 측정),
     prefers-reduced-motion guard (선택), responsive px width 정책 결정.
   룰북 = `wave-1-6-phase-c-precedent.md` per-leaf + 신규 audit `wave-1-6-voc-badge-audit.md` §7 acceptance.
   - **B-add-2** (token-only PR): 7 chip 토큰을 `frontend/src/tokens.ts` SSOT에 추가 + `index.css :root` 미러 +
     기존 raw `9999px` literals (Tag Pill `uidesign §5`, Filter Tab) → `var(--chip-radius-pill)` 마이그.
     Acceptance: `grep -E 'chip' frontend/src/tokens.ts` ↔ `grep -E '\-\-chip-' index.css` 7개 토큰 값 일치.
   - **C-2.6**: `shared/ui/badge/{TextMark,OutlineChip,SolidChip}.tsx` 신규 (TDD) + 기존 VocStatus/PriorityBadge
     refactor (Tailwind weight class → 토큰 기반, 5+5 기존 테스트 유지) + VocTypeBadge 신설 (dynamic contract +
     unknown slug fallback 테스트). ESLint `no-restricted-imports` + CI grep guard로 `interactive?: boolean` 차단.
   - C-3 = VocTagPill (OutlineChip wrapper, `#` glyph), C-4 = VocSubRow (기존 wrapper만 사용, 새 badge 0개).
   - **C-2 디자인 정책 적용 영향**: 향후 leaf 들 chip vs text-only 격상은 **사용자 시각 검수에서
     판단** (precedent rulebook §3 항목 5). 무조건 chip 가정 금지.
   - 알려진 follow-up:
     - badge-family `font-size` 통일 (C-1 status-badge 11px vs C-2 priority-badge 11.5px) →
       Phase D 일괄 정리 또는 별도 micro-PR.
     - mock data에 priority `low` 미포함 (`MSW handlers` cycle 3-step) → mock 보강 follow-up.
     - `p-${priority}` 암묵 className contract → 명시적 cssClass 필드 도입 (precedent doc 업데이트).
     - `--status-*` 토큰 `tokens.ts` SSOT 마이그 (chip 외) → C-2.6 후속 별도 refactoring PR.
     - uidesign.md heading에 stable slug anchor 도입 (§11~§14) → 차후 renumber 시 cross-ref sweep 방지.

**Wave 1.6 전체 흐름** (정본 plan: `docs/specs/plans/wave-1-6-voc-parity.md`)
- ✅ Phase A — 분해 산출물 (PR #126)
- ✅ Phase B — 토큰 갭 채우기 (PR #128) + violet 보강 (PR #132)
- 🟡 Phase C — 컴포넌트 rebuild (α 완료: C-1~C-7 + C-2.5/2.6/B-add-2 ✅ / β 완료: C-8 ✅ + C-9 ✅ + C-10 ✅ + F-1/F-2/F-3 bundle ✅ + F-bundle minor cleanup ✅ + #155/#156/#162/#166 follow-up ✅ / γ 완료: **C-11 ✅** / δ ε ζ 미착수: C-12~C-19 8 PR)
- ⛔ Phase D — 종합 검증 (Phase C 전체 batch 머지 후에만 진입 가능 — 현재 차단)
- 금지: Wave 2 + Follow-up C-2 hard-block (Wave 1.6 종료 전)

**Wave 1.5-γ 후속 (defer 항목, 별도 PR)**:

- VocCreateModal 수동 useState → RHF+Zod resolver 마이그레이션 (spec drift)
- DOMPurify customHTMLSanitizer (defense-in-depth)
- VocAdvancedFilters tag/voc_type chip 테스트 보강
- NativeSelect 단위 테스트
- systems/menus placeholder UUID → Wave 3 admin masters에서 query 연결
- openapi guard 확장 — VocListQuery + masters 3 schema 정합 가드

**Wave 1.6 follow-up (Phase D 또는 별도 PR)**:

- a11y audit — C1.D5 aria-label 중복 패턴 재검토 (시각 텍스트 + aria-label 중복 가능성)
- eslint `no-restricted-imports` — `shared/contracts/voc/*` deep path 금지 (C1.D6 enforcement)
- grep-based CI — `shared/contracts/**`/`backend/**`/`frontend/src/lib/api/**`에 `VOC_*_SLUG` 0 hits (C1.D2 enforcement)
- `--status-purple` (hue 290°, 0 callers) 사용 시점 — `--status-processing` family와의 hue 공유 재검토
- ~~**Issue #166** — sticky-header ↔ AppShell `<main>` padding 결합을 시맨틱 토큰으로 정리~~ → **MERGED** (PR #169).
- **Issue #166 후속 후보** — `frontend/src/components/voc/vocGridLayout.ts` 의 `VOC_GRID_PADDING_X = 'var(--sp-5)'` 가
  AppShell `--app-main-pad` 와 시각적으로 동일 (24px) 하지만 의도 결합 여부 미정. row 가장자리 정렬을
  의도한다면 별도 시맨틱 토큰 승격 필요. 별도 issue 가치 판단 후 결정.

**Wave 2 prerequisites**: Follow-up A (Playwright) + C-2 (seed UUID) 종결.
