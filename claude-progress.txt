# vocpage — Session Progress

## 다음 세션 시작점 (2026-05-09 PR-α 작성 완료)

→ **PR-α 작성 완료 (2026-05-09)** — `docs/wave-3-oq-sync`. OQ-1~5 close (사용자 grill) + ADR 0004/0005 Accepted + 11 파일 spec sync (ADR 2 + plans 4 + requires 4 + progress 1). uidesign.md leak grep 0 net new 유지 (baseline 2 hits 그대로). 사용자 PR open 대기.

→ **다음 세션 우선: 남은 2 PR 병렬 작성 (Wave 4 hotfix)**
   - **PR-β**  `fix/fu-001-dompurify`          — dompurify + SafeHtml wrapper, 4 surfaces (XSS, 가장 시급) — FU-001
   - **PR-γ**  `fix/fu-002-notice-faq-indexes` — 마이그 018 (notices/faqs hot-path 인덱스) — FU-002
   2 worktree 동시 작성, 의존성 0, 머지 순서 무관.

→ **머지 후 진입 순서** (PR-α 머지 후)
   1. Wave 3 Phase A — **4 PR full parallel**: W3-1=마이그 014 / W3-2=마이그 015 / W3-3=contract zod+openapi / W3-9=마이그 017
   2. Phase B+C 병렬 (Tag Master + Trash) → Phase D+E 병렬 (External Masters + Users) → Phase F 종합 검증
   3. FU-003/4/5 = Phase A 머지 후 idle slot 에서 hotfix-style. FU-006~009(P2) = Wave 3 close 후 batch.
   4. Wave 5 plan = Wave 3 close 후 작성 진입.

→ **Grill 결정 정합 (19건, 2026-05-09)**
   - **OQ-1** Tag Master mutate = **Option D** (Manager add/edit, Admin only merge/외부잠금/영구삭제)
   - **OQ-2** External Masters read = **B** (Manager+ + Dev)
   - **OQ-3** user_role_log = **A** (별 테이블, 마이그 017)
   - **OQ-4** 마이그 번호 = **014** Tag Master / **015** Trash / **017** user_role_log (013/016 점유)
   - **OQ-5** 사이드바 순서 = **B** (Result Review → Users → External Masters → Tag Master → Trash)
   - **ADR 0004** Accepted + 본문 재작성 (Option D 신설). **ADR 0005** Accepted (본문 유지).
   - **user_role_log schema** = `id, user_id, changed_by(NOT NULL), old_role, new_role, old_active, new_active, reason, created_at` + `(user_id, created_at DESC)` idx
   - **FU-001~009** 모두 등록 (P1 5 + P2 4). FU-001 = dompurify+SafeHtml 4 surfaces. FU-002 = 마이그 018.
   - **새 phase 생성 X** — uidesign cleanup 은 별 PR-α2 로 본 다음 세션 우선 처리, 그 뒤는 위 계획 그대로.

→ **Wave 4 (공지/FAQ/Notice popup) 머지 완료** — PR #245 (08070cc, 2026-05-09). Wave 3 plan draft = PR #244 머지.

→ **D-1~D-5 재실행 evidence (2026-05-09)**
   - D-1 기능: 변경 없음 (M-1·M-3 docs/test only). M-2 onError 토스트 추가 / L-1 다탭 동기화 / L-2 secondary sort.
   - D-2 시각: token-only — `npm run lint -w frontend` 0 warnings.
   - D-3 테스트: BE 116/123 PASS (+5 M-1) · FE 472/472 PASS (+3 L-1·L-2) · 7 todo 동일.
   - D-4 무결성: shared/contracts/openapi 변경 없음 (M-3 changelog only). fixture-seed parity 영향 없음.
   - D-5 운영: `voc-completion-decisions.md` 5 changelog row append, 본 파일 + next-session-tasks 동기화.

→ **Phase 2 (분리 PR) — 적대적 리뷰 결과**
   §9.2.2 VOC 목록 인라인 서브태스크 펼침은 본 PR 에 끼우지 않음.
   근거: critic + codex 양측 합의 — Wave 1.6 close PR 정체성 / D-1~D-5 evidence stale 위험 / BE `total` 의미 silent change /
   zod self-recursive (`VocListItem.subTasks`) 함정 / status grouping × expand 충돌 / openapi 동기화 / Playwright deferred.

## Active Waves

**`/voc` 완성 — 단일 PR (PR open 대기)**
- ✅ 12 commit, BE/FE green, follow-up 5건 반영
- ⏳ 사용자 PR open + merge → Phase 2 진입

**Phase 2 — `feat/voc-list-expand`** (§9.2.2 D16, 2026-04-26)
- ⛔ `/voc` 완성 PR 머지 후 진입. ADR 선행 (total 의미 · 자기참조 zod · grouping 배타 · 자식 페이로드 cap).

**Wave 2 — Dashboard** (정본: `docs/specs/requires/dashboard.md`)
- ⛔ Phase 2 머지 후 진입.
