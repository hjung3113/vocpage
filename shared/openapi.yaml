openapi: 3.1.0
info:
  title: VOCPage API
  version: 0.0.1
  description: VOC (Voice of Customer) management system REST API

servers:
  - url: /api
    description: Local dev

tags:
  - name: auth
  - name: health
  - name: vocs
  - name: comments
  - name: notes
  - name: attachments
  - name: tags
  - name: notifications
  - name: dashboard
  - name: admin
  - name: files

paths:
  /health:
    get:
      tags: [health]
      operationId: getHealth
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [status, db, uptime]
                properties:
                  status:
                    type: string
                    enum: [ok]
                  db:
                    type: string
                    enum: [ok]
                  uptime:
                    type: number

  /auth/mock-login:
    post:
      tags: [auth]
      operationId: mockLogin
      summary: Mock login (dev only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role:
                  type: string
                  enum: [admin, manager, dev, user]
      responses:
        '200':
          description: Logged in
          content:
            application/json:
              schema:
                type: object
                required: [user]
                properties:
                  user:
                    $ref: '#/components/schemas/AuthUser'
        '400':
          $ref: '#/components/responses/BadRequest'

  /auth/logout:
    post:
      tags: [auth]
      operationId: logout
      summary: Logout
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'

  /auth/me:
    get:
      tags: [auth]
      operationId: getMe
      summary: Get current user
      responses:
        '200':
          description: Current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthUser'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /vocs:
    get:
      tags: [vocs]
      operationId: listVocs
      summary: List VOCs (paginated)
      parameters:
        - $ref: '#/components/parameters/page'
        - name: per_page
          in: query
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
        - name: sort_by
          in: query
          schema:
            type: string
            enum: [issue_code, title, status, assignee, priority, created_at]
            default: created_at
        - name: sort_dir
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
        - name: status
          in: query
          description: Repeat to filter by multiple statuses.
          schema:
            type: array
            items:
              $ref: '#/components/schemas/VocStatus'
          style: form
          explode: true
        - name: priorities
          in: query
          schema:
            type: array
            items:
              $ref: '#/components/schemas/VocPriority'
          style: form
          explode: true
        - name: system_id
          in: query
          schema:
            type: string
            format: uuid
        - name: voc_type_ids
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          style: form
          explode: true
        - name: assignees
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          style: form
          explode: true
        - name: tag_ids
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
          style: form
          explode: true
        - name: q
          in: query
          schema:
            type: string
            maxLength: 120
        - name: includeDeleted
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Paginated VOC list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedVocs'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [vocs]
      operationId: createVoc
      summary: Create VOC
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VocInput'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Voc'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /vocs/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [vocs]
      operationId: getVoc
      summary: Get VOC detail
      responses:
        '200':
          description: VOC
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Voc'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [vocs]
      operationId: updateVoc
      summary: Update VOC
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VocPatch'
      responses:
        '200':
          description: Updated VOC
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Voc'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [vocs]
      operationId: deleteVoc
      summary: Soft-delete VOC (admin only)
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/status:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [vocs]
      operationId: updateVocStatus
      summary: Update VOC status
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  $ref: '#/components/schemas/VocStatus'
      responses:
        '200':
          description: Updated VOC
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Voc'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/comments:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [comments]
      operationId: listComments
      summary: List comments for a VOC (created_at ASC, max 200)
      description: |
        Returns up to 200 comments ordered by created_at ASC.
        Requires VOC read access. Internal notes are never included.
        See feature-voc.md §8.13.
      responses:
        '200':
          description: Comment list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommentListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [comments]
      operationId: createComment
      summary: Add comment to VOC
      description: |
        Any authenticated user may comment on a non-deleted VOC.
        Body is HTML (Toast UI output), max 16 KB. Empty or
        whitespace-only HTML is rejected with 422 BODY_REQUIRED.
        Fires notifyOnComment for author + assignee (debounced 5 min).
        See feature-voc.md §8.13.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCommentInput'
      responses:
        '201':
          description: Comment created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommentResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Validation error (BODY_REQUIRED or BODY_TOO_LARGE)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /vocs/{id}/comments/{commentId}:
    parameters:
      - $ref: '#/components/parameters/id'
      - name: commentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags: [comments]
      operationId: updateComment
      summary: Update comment (author or admin only)
      description: |
        Author or admin may update a comment body. updated_at is auto-refreshed.
        Empty body → 422 BODY_REQUIRED. See feature-voc.md §8.13.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCommentInput'
      responses:
        '200':
          description: Updated comment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CommentResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          description: Validation error (BODY_REQUIRED or BODY_TOO_LARGE)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [comments]
      operationId: deleteComment
      summary: Hard-delete comment (author or admin only)
      description: |
        Hard delete — no soft delete, no audit log entry (spec §8.13).
        Author or admin only. See feature-voc.md §8.13.
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/notes:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [notes]
      operationId: listNotes
      summary: List internal notes (manager/admin only)
      responses:
        '200':
          description: Notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Note'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [notes]
      operationId: createNote
      summary: Add internal note (manager/admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
      responses:
        '201':
          description: Created note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /vocs/{id}/notes/{noteId}:
    parameters:
      - $ref: '#/components/parameters/id'
      - name: noteId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags: [notes]
      operationId: updateNote
      summary: Update internal note (manager/admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
      responses:
        '200':
          description: Updated note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Note'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [notes]
      operationId: deleteNote
      summary: Delete internal note (manager/admin only)
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/attachments:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [attachments]
      operationId: listAttachments
      summary: List attachments for VOC
      responses:
        '200':
          description: Attachments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Attachment'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [attachments]
      operationId: uploadAttachment
      summary: Upload attachment
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Created attachment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attachment'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /vocs/{id}/attachments/{attachmentId}:
    parameters:
      - $ref: '#/components/parameters/id'
      - name: attachmentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      tags: [attachments]
      operationId: deleteAttachment
      summary: Delete attachment
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /tags:
    get:
      tags: [tags]
      operationId: listTags
      summary: List all tags
      responses:
        '200':
          description: Tags
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Tag'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /masters/assignees:
    get:
      tags: [masters]
      operationId: listAssignees
      summary: List assignable users (admin/manager/dev) for filter dropdowns
      responses:
        '200':
          description: Assignees
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssigneeListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /masters/tags:
    get:
      tags: [masters]
      operationId: listMasterTags
      summary: List tags for filter dropdowns
      responses:
        '200':
          description: Tags
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /masters/voc-types:
    get:
      tags: [masters]
      operationId: listMasterVocTypes
      summary: List VOC types for filter dropdowns
      responses:
        '200':
          description: VOC types
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VocTypeListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /masters/systems:
    get:
      tags: [masters]
      operationId: listMasterSystems
      summary: List active systems with nested active menus (VOC create cascade)
      responses:
        '200':
          description: Systems with nested menus (archived rows excluded)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /masters/menus:
    get:
      tags: [masters]
      operationId: listMasterMenus
      summary: List menus (admin only — supports system_id filter and includeArchived)
      parameters:
        - in: query
          name: system_id
          schema:
            type: string
            format: uuid
        - in: query
          name: includeArchived
          schema:
            type: boolean
      responses:
        '200':
          description: Menus
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MenuListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /vocs/{id}/tags:
    parameters:
      - $ref: '#/components/parameters/id'
    post:
      tags: [tags]
      operationId: addVocTag
      summary: Add tag to VOC
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tagId]
              properties:
                tagId:
                  type: string
                  format: uuid
      responses:
        '200':
          description: Tag added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /vocs/{id}/tags/{tagId}:
    parameters:
      - $ref: '#/components/parameters/id'
      - name: tagId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      tags: [tags]
      operationId: removeVocTag
      summary: Remove tag from VOC
      responses:
        '200':
          description: Tag removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /notifications:
    get:
      tags: [notifications]
      operationId: listNotifications
      summary: List notifications for current user (with isUrgent join + lazy trim)
      description: |
        Returns up to 50 most recent notifications. Lazy trim runs on every
        call: deletes rows older than 30 days and trims per-user beyond the
        50-row cap (Wave 5 W5-D4). `isUrgent` is computed at read time from
        the joined `vocs.priority` (W5-D2) — Urgent demotion auto-clears the
        flag on the next call.
      responses:
        '200':
          description: Notifications
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /notifications/unread-count:
    get:
      tags: [notifications]
      operationId: getUnreadCount
      summary: Get unread notification count (ETag/304)
      description: |
        Returns the unread count for the current user. Response carries an
        ETag derived from `(count, latest_unread_id, latest_unread_created_at)`.
        Conditional GET with `If-None-Match` returns 304 with no body
        (Wave 5 W5-D7, 30-second polling endpoint).
      parameters:
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Count
          headers:
            ETag:
              schema:
                type: string
              description: Strong-ish hash of the unread state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnreadCountResponse'
        '304':
          description: Not Modified — unread state unchanged since the supplied ETag.
        '401':
          $ref: '#/components/responses/Unauthorized'

  /notifications/mark-all-read:
    post:
      tags: [notifications]
      operationId: markAllRead
      summary: Mark every unread notification of the current user as read
      responses:
        '200':
          description: Marked all read
          content:
            application/json:
              schema:
                type: object
                required: [unreadCount]
                properties:
                  unreadCount:
                    type: integer
                    enum: [0]
        '401':
          $ref: '#/components/responses/Unauthorized'

  /notifications/{id}/read:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [notifications]
      operationId: markNotificationRead
      summary: Mark notification as read
      responses:
        '200':
          description: Marked read
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /dashboard/summary:
    get:
      tags: [dashboard]
      operationId: getDashboardSummary
      summary: Get KPI summary (8 metrics)
      parameters:
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: KPI summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/distribution:
    get:
      tags: [dashboard]
      operationId: getDashboardDistribution
      summary: Get distribution by type
      parameters:
        - name: type
          in: query
          required: true
          schema:
            type: string
            enum: [status, priority, voc_type, tag]
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Distribution array
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DistributionItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/priority-status-matrix:
    get:
      tags: [dashboard]
      operationId: getPriorityStatusMatrix
      summary: Get priority-status matrix
      parameters:
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Matrix
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/heatmap:
    get:
      tags: [dashboard]
      operationId: getDashboardHeatmap
      summary: Get heatmap data
      parameters:
        - name: xAxis
          in: query
          schema:
            type: string
            enum: [status, priority, tag]
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Heatmap
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/weekly-trend:
    get:
      tags: [dashboard]
      operationId: getWeeklyTrend
      summary: Get weekly trend
      parameters:
        - name: weeks
          in: query
          schema:
            type: integer
            default: 12
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Trend array
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WeeklyTrendItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/tag-distribution:
    get:
      tags: [dashboard]
      operationId: getTagDistribution
      summary: Get tag distribution
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Tag stats
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TagStatItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/system-overview:
    get:
      tags: [dashboard]
      operationId: getSystemOverview
      summary: Get system overview stats
      parameters:
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: System stats
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SystemStatItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/assignee-stats:
    get:
      tags: [dashboard]
      operationId: getAssigneeStats
      summary: Get assignee stats
      parameters:
        - name: xAxis
          in: query
          schema:
            type: string
            enum: [status, priority, tag]
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Assignee stats
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/processing-speed:
    get:
      tags: [dashboard]
      operationId: getProcessingSpeed
      summary: Get processing speed (SLA table)
      parameters:
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: SLA table
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/aging:
    get:
      tags: [dashboard]
      operationId: getAging
      summary: Get aging bar data
      parameters:
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Aging bar data
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/aging-vocs:
    get:
      tags: [dashboard]
      operationId: getAgingVocs
      summary: Get top aging VOCs
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - $ref: '#/components/parameters/dashboardSystemId'
        - $ref: '#/components/parameters/dashboardMenuId'
        - $ref: '#/components/parameters/dashboardAssigneeId'
        - $ref: '#/components/parameters/dashboardStartDate'
        - $ref: '#/components/parameters/dashboardEndDate'
      responses:
        '200':
          description: Top aging VOCs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Voc'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/settings:
    get:
      tags: [dashboard]
      operationId: getDashboardSettings
      summary: Get dashboard settings
      responses:
        '200':
          description: Settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardSettings'
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      tags: [dashboard]
      operationId: updateDashboardSettings
      summary: Update dashboard settings (locked_fields/globaltabs_order are admin-only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DashboardSettingsUpdate'
      responses:
        '200':
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardSettings'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /dashboard/menus:
    get:
      tags: [dashboard]
      operationId: getDashboardMenus
      summary: Get menus for dashboard filter
      parameters:
        - name: systemId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Menus
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Menu'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /dashboard/assignees:
    get:
      tags: [dashboard]
      operationId: getDashboardAssignees
      summary: Get assignees for dashboard filter
      responses:
        '200':
          description: Users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /admin/users:
    get:
      tags: [admin]
      operationId: adminListUsers
      summary: List users (admin only — paginated; OQ-3 audit via /role-log)
      parameters:
        - name: role
          in: query
          schema: { $ref: '#/components/schemas/UserRole' }
        - name: is_active
          in: query
          schema: { type: boolean }
        - name: q
          in: query
          schema: { type: string }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: per_page
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Users
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminUserListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/users/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminUpdateUser
      summary: Update user role/is_active (admin only — last-admin guard 409)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminUserPatch'
      responses:
        '200':
          description: Updated user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminUserItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/BadRequest'

  /admin/systems:
    get:
      tags: [admin]
      operationId: adminListSystems
      summary: List systems (admin)
      responses:
        '200':
          description: Systems
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/System'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [admin]
      operationId: adminCreateSystem
      summary: Create system (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SystemInput'
      responses:
        '201':
          description: Created system
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/System'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/systems/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminUpdateSystem
      summary: Update system (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SystemPatch'
      responses:
        '200':
          description: Updated system
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/System'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /admin/menus:
    get:
      tags: [admin]
      operationId: adminListMenus
      summary: List menus (admin)
      parameters:
        - name: systemId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Menus
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Menu'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [admin]
      operationId: adminCreateMenu
      summary: Create menu (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MenuInput'
      responses:
        '201':
          description: Created menu
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Menu'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/menus/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminUpdateMenu
      summary: Update menu (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MenuPatch'
      responses:
        '200':
          description: Updated menu
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Menu'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /admin/voc-types:
    get:
      tags: [admin]
      operationId: adminListVocTypes
      summary: List VOC types (admin)
      responses:
        '200':
          description: VOC types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VocType'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [admin]
      operationId: adminCreateVocType
      summary: Create VOC type (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VocTypeInput'
      responses:
        '201':
          description: Created VOC type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VocType'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/voc-types/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminUpdateVocType
      summary: Update VOC type (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VocTypePatch'
      responses:
        '200':
          description: Updated VOC type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VocType'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /admin/tag-rules:
    get:
      tags: [admin]
      operationId: adminListTagRules
      summary: List tag rules (admin)
      responses:
        '200':
          description: Tag rules
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TagRule'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [admin]
      operationId: adminCreateTagRule
      summary: Create tag rule (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagRuleInput'
      responses:
        '201':
          description: Created tag rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagRule'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/tag-rules/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminUpdateTagRule
      summary: Update tag rule (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagRulePatch'
      responses:
        '200':
          description: Updated tag rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagRule'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [admin]
      operationId: adminDeleteTagRule
      summary: Delete tag rule (admin)
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /admin/masters/refresh:
    post:
      tags: [admin]
      operationId: adminRefreshMasters
      summary: Refresh master data cache (manager+)
      responses:
        '200':
          description: Refresh result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MasterRefreshResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/BadRequest'
        '503':
          $ref: '#/components/responses/BadRequest'

  /admin/masters/status:
    get:
      tags: [admin]
      operationId: adminMasterStatus
      summary: External masters status (admin/manager/dev — OQ-2 Option B)
      responses:
        '200':
          description: Master status snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MasterStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/tags:
    get:
      tags: [admin]
      operationId: adminListTags
      summary: List tag master rows (admin/manager/dev — read)
      parameters:
        - name: kind
          in: query
          schema:
            type: string
            enum: [general, menu]
        - name: q
          in: query
          schema: { type: string }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: per_page
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Tag master rows
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagMasterListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [admin]
      operationId: adminCreateTag
      summary: Create tag master row (manager+ — slug auto-generated)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagMasterCreate'
      responses:
        '201':
          description: Created tag
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagMasterItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/BadRequest'

  /admin/tags/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminUpdateTag
      summary: Update tag name (manager+; kind/slug locked)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagMasterPatch'
      responses:
        '200':
          description: Updated tag
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagMasterItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [admin]
      operationId: adminDeleteTag
      summary: Permanently delete tag (admin only — usage=0 + rules ref=0)
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/BadRequest'

  /admin/tags/{id}/merge:
    parameters:
      - $ref: '#/components/parameters/id'
    post:
      tags: [admin]
      operationId: adminMergeTag
      summary: Merge tag into target (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagMasterMergeInput'
      responses:
        '200':
          description: Merged target tag
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagMasterItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /admin/tags/{id}/external:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminToggleTagExternal
      summary: Toggle tag external lock (admin only — tags.is_external)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagExternalToggle'
      responses:
        '200':
          description: Updated tag
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagMasterItem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /admin/tag-rules/{id}/suspend:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [admin]
      operationId: adminSuspendTagRule
      summary: Suspend or resume tag rule (admin only — tag_rules.suspended_until)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TagRuleSuspendInput'
      responses:
        '200':
          description: Updated tag rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagRule'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /admin/vocs/trash:
    get:
      tags: [admin]
      operationId: adminListTrash
      summary: List soft-deleted VOCs (admin only — §15.4)
      parameters:
        - name: system_id
          in: query
          schema: { type: string, format: uuid }
        - name: menu_id
          in: query
          schema: { type: string, format: uuid }
        - name: q
          in: query
          schema: { type: string }
        - name: deleted_from
          in: query
          schema: { type: string, format: date-time }
        - name: deleted_to
          in: query
          schema: { type: string, format: date-time }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: per_page
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Trash list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrashListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          # ADR 0004 §3: Trash hidden from non-admin as 404 ("존재 자체 은닉"), not 403.
          $ref: '#/components/responses/NotFound'

  /admin/vocs/{id}/restore-log:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [admin]
      operationId: adminVocRestoreLog
      summary: Read voc_restore_log entries (admin only)
      responses:
        '200':
          description: Restore log entries (oldest → newest)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VocRestoreLogEntry'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          # ADR 0005: hidden from non-admin as 404 ("존재 자체 은닉"), not 403.
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/id'
    patch:
      tags: [vocs]
      operationId: restoreVoc
      summary: Restore soft-deleted VOC (admin only — re-runs tag_rules)
      responses:
        '200':
          description: Restored VOC + audit row
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VocRestoreResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          # ADR 0005: hidden from non-admin as 404, not 403.
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /admin/users/{id}/role-log:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [admin]
      operationId: adminUserRoleLog
      summary: Read user_role_log entries (admin only — PIPA 7y)
      responses:
        '200':
          description: Role log entries (newest → oldest)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserRoleLogEntry'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/masters/refresh:
    parameters:
      - $ref: '#/components/parameters/id'
    post:
      tags: [vocs]
      operationId: refreshVocMasters
      summary: Refresh master data for a VOC
      responses:
        '200':
          description: Refresh result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MasterRefreshResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/payload:
    parameters:
      - $ref: '#/components/parameters/id'
    post:
      tags: [vocs]
      operationId: submitVocPayload
      summary: Submit structured payload for a VOC
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [equipment, maker, model, process, symptom, root_cause, resolution]
              properties:
                equipment:
                  type: string
                maker:
                  type: string
                model:
                  type: string
                process:
                  type: string
                symptom:
                  type: string
                root_cause:
                  type: string
                resolution:
                  type: string
                unverified_fields:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Submitted payload history entry
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VocPayloadHistory'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/payload-history:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [vocs]
      operationId: getVocPayloadHistory
      summary: Get payload submission history for a VOC
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Payload history list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/VocPayloadHistory'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/payload-review:
    parameters:
      - $ref: '#/components/parameters/id'
    post:
      tags: [vocs]
      operationId: reviewVocPayload
      summary: Approve or reject a VOC payload (Result Review)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [decision]
              properties:
                decision:
                  type: string
                  enum: ['approve', 'reject']
                comment:
                  type: string
                  nullable: true
                  description: required when decision='reject'
      responses:
        '201':
          description: Review recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /vocs/{id}/timeline:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [vocs]
      operationId: getVocTimeline
      summary: Get VOC timeline events (Manager/Admin only)
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Timeline event list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /files/{id}:
    parameters:
      - $ref: '#/components/parameters/id'
    get:
      tags: [files]
      operationId: downloadFile
      summary: Download file by ID
      security:
        - bearerAuth: []
      responses:
        '200':
          description: File binary content
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /notices:
    get:
      tags: [notices]
      operationId: listNotices
      summary: List notices (visibility-filtered for non-admin)
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: per_page
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: mode
          in: query
          schema: { type: string, enum: [user, admin], default: user }
        - name: includeDeleted
          in: query
          schema: { type: boolean }
      responses:
        '200':
          description: Notice list
          content:
            application/json:
              schema:
                type: object
                properties:
                  rows:
                    type: array
                    items: { $ref: '#/components/schemas/Notice' }
                  page: { type: integer }
                  per_page: { type: integer }
                  total: { type: integer }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [notices]
      operationId: createNotice
      summary: Create notice (admin/manager)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/NoticeInput' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Notice' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /notices/popup:
    get:
      tags: [notices]
      operationId: listPopupNotices
      summary: Login popup notices (BE stateless; FE owns dismiss state via localStorage per spec §10.3.2)
      responses:
        '200':
          description: Popup notices
          content:
            application/json:
              schema:
                type: object
                properties:
                  rows:
                    type: array
                    items: { $ref: '#/components/schemas/Notice' }
        '401': { $ref: '#/components/responses/Unauthorized' }

  /notices/{id}:
    get:
      tags: [notices]
      operationId: getNotice
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: Notice
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Notice' }
        '404': { $ref: '#/components/responses/NotFound' }
    patch:
      tags: [notices]
      operationId: updateNotice
      parameters: [{ $ref: '#/components/parameters/id' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/NoticeInput' }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Notice' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [notices]
      operationId: deleteNotice
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '204': { description: Soft-deleted }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /notices/{id}/restore:
    post:
      tags: [notices]
      operationId: restoreNotice
      summary: Restore soft-deleted notice (admin only)
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Notice' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /faqs:
    get:
      tags: [faqs]
      operationId: listFaqs
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: per_page
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: category_id
          in: query
          schema: { type: string, format: uuid }
        - name: q
          in: query
          schema: { type: string, maxLength: 120 }
        - name: mode
          in: query
          schema: { type: string, enum: [user, admin], default: user }
        - name: includeDeleted
          in: query
          schema: { type: boolean }
      responses:
        '200':
          description: FAQ list
          content:
            application/json:
              schema:
                type: object
                properties:
                  rows:
                    type: array
                    items: { $ref: '#/components/schemas/Faq' }
                  page: { type: integer }
                  per_page: { type: integer }
                  total: { type: integer }
    post:
      tags: [faqs]
      operationId: createFaq
      summary: Create FAQ (admin/manager)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/FaqInput' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Faq' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /faqs/{id}:
    get:
      tags: [faqs]
      operationId: getFaq
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: FAQ
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Faq' }
        '404': { $ref: '#/components/responses/NotFound' }
    patch:
      tags: [faqs]
      operationId: updateFaq
      parameters: [{ $ref: '#/components/parameters/id' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/FaqInput' }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Faq' }
        '403': { $ref: '#/components/responses/Forbidden' }
    delete:
      tags: [faqs]
      operationId: deleteFaq
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '204': { description: Soft-deleted }
        '403': { $ref: '#/components/responses/Forbidden' }

  /faqs/{id}/restore:
    post:
      tags: [faqs]
      operationId: restoreFaq
      summary: Restore soft-deleted FAQ (admin only)
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Faq' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /faq-categories:
    get:
      tags: [faqs]
      operationId: listFaqCategories
      responses:
        '200':
          description: Categories
          content:
            application/json:
              schema:
                type: object
                properties:
                  rows:
                    type: array
                    items: { $ref: '#/components/schemas/FaqCategory' }
    post:
      tags: [faqs]
      operationId: createFaqCategory
      summary: Create FAQ category (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/FaqCategoryInput' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/FaqCategory' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /faq-categories/{id}:
    patch:
      tags: [faqs]
      operationId: updateFaqCategory
      parameters: [{ $ref: '#/components/parameters/id' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/FaqCategoryInput' }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema: { $ref: '#/components/schemas/FaqCategory' }
        '403': { $ref: '#/components/responses/Forbidden' }
    delete:
      tags: [faqs]
      operationId: deleteFaqCategory
      summary: Delete category (admin only). Returns 409 CATEGORY_HAS_ITEMS if FAQs are still attached.
      parameters: [{ $ref: '#/components/parameters/id' }]
      responses:
        '204': { description: Deleted }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409':
          description: Category has FAQ items
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErrorResponse' }

components:
  parameters:
    id:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    page:
      name: page
      in: query
      schema:
        type: integer
        default: 1
        minimum: 1
    limit:
      name: limit
      in: query
      schema:
        type: integer
        default: 20
        minimum: 1
        maximum: 100
    dashboardSystemId:
      name: systemId
      in: query
      schema:
        type: string
        format: uuid
    dashboardMenuId:
      name: menuId
      in: query
      schema:
        type: string
        format: uuid
    dashboardAssigneeId:
      name: assigneeId
      in: query
      schema:
        type: string
        format: uuid
    dashboardStartDate:
      name: startDate
      in: query
      schema:
        type: string
        format: date
    dashboardEndDate:
      name: endDate
      in: query
      schema:
        type: string
        format: date

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    OkResponse:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
          enum: [true]

    ErrorResponse:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true

    UserRole:
      type: string
      enum: [user, manager, admin, dev]

    AuthUser:
      type: object
      required: [id, display_name, email, role]
      properties:
        id:
          type: string
          format: uuid
        ad_username:
          type: string
          nullable: true
        display_name:
          type: string
        email:
          type: string
          format: email
        role:
          $ref: '#/components/schemas/UserRole'

    User:
      type: object
      required: [id, ad_username, display_name, email, role, is_active]
      properties:
        id:
          type: string
          format: uuid
        ad_username:
          type: string
        display_name:
          type: string
        email:
          type: string
          format: email
        role:
          $ref: '#/components/schemas/UserRole'
        is_active:
          type: boolean

    VocStatus:
      type: string
      enum: ['접수', '검토중', '처리중', '완료', '드랍']

    VocPriority:
      type: string
      enum: [urgent, high, medium, low]

    VocSource:
      type: string
      enum: [manual, import]

    Voc:
      type: object
      required: [id, issue_code, sequence_no, title, status, priority, voc_type_id, system_id, menu_id, source, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        issue_code:
          type: string
        sequence_no:
          type: integer
        title:
          type: string
        body:
          type: string
          nullable: true
        status:
          $ref: '#/components/schemas/VocStatus'
        priority:
          $ref: '#/components/schemas/VocPriority'
        voc_type_id:
          type: string
          format: uuid
        system_id:
          type: string
          format: uuid
        menu_id:
          type: string
          format: uuid
        assignee_id:
          type: string
          format: uuid
          nullable: true
        author_id:
          type: string
          format: uuid
        parent_id:
          type: string
          format: uuid
          nullable: true
        source:
          $ref: '#/components/schemas/VocSource'
        review_status:
          type: string
          nullable: true
        structured_payload:
          type: object
          additionalProperties: true
          nullable: true
        resolution_quality:
          type: string
          enum: ['근본해결', '임시조치']
          nullable: true
        drop_reason:
          type: string
          enum: ['중복', '정책거부', '재현불가', '범위외', '기타']
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        due_date:
          type: string
          format: date-time
          nullable: true
        deleted_at:
          type: string
          format: date-time
          nullable: true

    VocInput:
      type: object
      required: [title, status, priority, voc_type_id, system_id, menu_id]
      properties:
        title:
          type: string
        body:
          type: string
        status:
          $ref: '#/components/schemas/VocStatus'
        priority:
          $ref: '#/components/schemas/VocPriority'
        voc_type_id:
          type: string
          format: uuid
        system_id:
          type: string
          format: uuid
        menu_id:
          type: string
          format: uuid
        assignee_id:
          type: string
          format: uuid
        parent_id:
          type: string
          format: uuid
        source:
          $ref: '#/components/schemas/VocSource'

    VocPatch:
      type: object
      properties:
        title:
          type: string
        body:
          type: string
        status:
          $ref: '#/components/schemas/VocStatus'
        priority:
          $ref: '#/components/schemas/VocPriority'
        voc_type_id:
          type: string
          format: uuid
        system_id:
          type: string
          format: uuid
        menu_id:
          type: string
          format: uuid
        assignee_id:
          type: string
          format: uuid
        review_status:
          type: string
          enum: ['unverified', 'approved', 'rejected', 'pending_deletion']
        resolution_quality:
          type: string
          enum: ['근본해결', '임시조치']
        drop_reason:
          type: string
          enum: ['중복', '정책거부', '재현불가', '범위외', '기타']

    Comment:
      type: object
      required: [id, voc_id, author_id, body, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        voc_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
        body:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CommentListResponse:
      type: object
      required: [rows]
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/Comment'

    CommentResponse:
      type: object
      required: [comment]
      properties:
        comment:
          $ref: '#/components/schemas/Comment'

    CreateCommentInput:
      type: object
      required: [body]
      properties:
        body:
          type: string
          description: HTML string (Toast UI output). Max 16 KB. Empty / whitespace-only rejected.
          maxLength: 16384

    Note:
      type: object
      required: [id, voc_id, author_id, body, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        voc_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
        body:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        deleted_at:
          type: string
          format: date-time
          nullable: true

    Attachment:
      type: object
      required: [id, voc_id, filename, original_name, mime_type, size, url, created_at]
      properties:
        id:
          type: string
          format: uuid
        voc_id:
          type: string
          format: uuid
        filename:
          type: string
        original_name:
          type: string
        mime_type:
          type: string
        size:
          type: integer
        url:
          type: string
        created_at:
          type: string
          format: date-time

    Tag:
      type: object
      required: [id, name, slug, kind, created_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        kind:
          type: string
          enum: [general, menu]
        color:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time

    System:
      type: object
      required: [id, name, slug, is_archived]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        is_archived:
          type: boolean

    SystemInput:
      type: object
      required: [name, slug]
      properties:
        name:
          type: string
        slug:
          type: string

    SystemPatch:
      type: object
      properties:
        name:
          type: string
        slug:
          type: string
        is_archived:
          type: boolean

    Menu:
      type: object
      required: [id, system_id, name, slug, is_archived]
      properties:
        id:
          type: string
          format: uuid
        system_id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        is_archived:
          type: boolean

    MenuInput:
      type: object
      required: [system_id, name, slug]
      properties:
        system_id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string

    MenuPatch:
      type: object
      properties:
        name:
          type: string
        slug:
          type: string
        is_archived:
          type: boolean

    VocType:
      type: object
      required: [id, name, slug, sort_order, is_archived]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        color:
          type: string
          nullable: true
        sort_order:
          type: integer
        is_archived:
          type: boolean

    VocTypeInput:
      type: object
      required: [name, slug]
      properties:
        name:
          type: string
        slug:
          type: string
        color:
          type: string
        sort_order:
          type: integer

    VocTypePatch:
      type: object
      properties:
        name:
          type: string
        slug:
          type: string
        color:
          type: string
        sort_order:
          type: integer
        is_archived:
          type: boolean

    TagRule:
      type: object
      required: [id, name, pattern, kind, tag_id, is_active]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        pattern:
          type: string
        kind:
          type: string
          enum: ['general']
        tag_id:
          type: string
          format: uuid
        is_active:
          type: boolean

    TagRuleInput:
      type: object
      required: [name, pattern, tag_id]
      properties:
        name:
          type: string
        pattern:
          type: string
        kind:
          type: string
          enum: ['general']
          default: general
        tag_id:
          type: string
          format: uuid
        is_active:
          type: boolean

    TagRulePatch:
      type: object
      properties:
        name:
          type: string
        pattern:
          type: string
        kind:
          type: string
          enum: ['general']
        tag_id:
          type: string
          format: uuid
        is_active:
          type: boolean

    Notification:
      type: object
      required: [id, type, label, createdAt, read]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          # `mention` retained for FE backward compat (Wave 1.5 PR-β fixtures);
          # BE only emits the three DB-CHECK constrained values.
          enum: ['comment', 'status_change', 'assigned', 'mention']
        label:
          type: string
        href:
          type: string
        createdAt:
          type: string
          format: date-time
        read:
          type: boolean
        # Wave 5 Phase A — VOC backref + Urgent flag (priority join).
        voc_id:
          type: string
          format: uuid
        voc_issue_code:
          type: string
        voc_title:
          type: string
        isUrgent:
          type: boolean
    NotificationListResponse:
      type: object
      required: [items, unreadCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Notification'
        unreadCount:
          type: integer
          minimum: 0
    UnreadCountResponse:
      type: object
      required: [count]
      properties:
        count:
          type: integer
          minimum: 0

    VocListItem:
      type: object
      required: [id, issue_code, title, status, priority, voc_type_id, system_id, menu_id, assignee_id, author_id, parent_id, source, due_date, created_at, updated_at, has_children, notes_count, tags]
      properties:
        id:
          type: string
          format: uuid
        issue_code:
          type: string
        title:
          type: string
        status:
          $ref: '#/components/schemas/VocStatus'
        priority:
          $ref: '#/components/schemas/VocPriority'
        voc_type_id:
          type: string
          format: uuid
        system_id:
          type: string
          format: uuid
        menu_id:
          type: string
          format: uuid
        assignee_id:
          type: string
          format: uuid
          nullable: true
        author_id:
          type: string
          format: uuid
        parent_id:
          type: string
          format: uuid
          nullable: true
        source:
          $ref: '#/components/schemas/VocSource'
        due_date:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        has_children:
          type: boolean
        notes_count:
          type: integer
          minimum: 0
        tags:
          type: array
          items:
            type: string

    PaginatedVocs:
      type: object
      required: [rows, total, page, per_page]
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/VocListItem'
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer

    AssigneeListItem:
      type: object
      required: [id, ad_username, display_name]
      properties:
        id:
          type: string
          format: uuid
        ad_username:
          type: string
        display_name:
          type: string

    AssigneeListResponse:
      type: object
      required: [rows]
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/AssigneeListItem'

    TagListItem:
      type: object
      required: [id, name, slug, kind]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        kind:
          type: string

    TagListResponse:
      type: object
      required: [rows]
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/TagListItem'

    VocTypeListItem:
      type: object
      required: [id, name, slug, color, sort_order, is_archived]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        color:
          type: string
          nullable: true
        sort_order:
          type: integer
        is_archived:
          type: boolean

    VocTypeListResponse:
      type: object
      required: [rows]
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/VocTypeListItem'

    MenuListItem:
      type: object
      required: [id, system_id, name, slug, is_archived]
      properties:
        id:
          type: string
          format: uuid
        system_id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        is_archived:
          type: boolean

    MenuListResponse:
      type: object
      required: [rows]
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/MenuListItem'

    SystemListItem:
      type: object
      required: [id, name, slug, is_archived, menus]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        is_archived:
          type: boolean
        menus:
          type: array
          items:
            $ref: '#/components/schemas/MenuListItem'

    SystemListResponse:
      type: object
      required: [rows]
      properties:
        rows:
          type: array
          items:
            $ref: '#/components/schemas/SystemListItem'

    DashboardSummary:
      type: object
      required: [total, open, in_progress, completed, dropped, urgent, overdue, avg_resolution_days]
      properties:
        total:
          type: integer
        open:
          type: integer
        in_progress:
          type: integer
        completed:
          type: integer
        dropped:
          type: integer
        urgent:
          type: integer
        overdue:
          type: integer
        avg_resolution_days:
          type: number

    RglLayoutItem:
      type: object
      required: [i, x, y, w, h]
      additionalProperties: false
      properties:
        i: { type: string, minLength: 1 }
        x: { type: integer, minimum: 0 }
        y: { type: integer, minimum: 0 }
        w: { type: integer, minimum: 1 }
        h: { type: integer, minimum: 1 }
        minW: { type: integer, minimum: 1 }
        minH: { type: integer, minimum: 1 }
        static: { type: boolean }

    RglLayouts:
      type: object
      additionalProperties: false
      properties:
        xs: { type: array, items: { $ref: '#/components/schemas/RglLayoutItem' } }
        sm: { type: array, items: { $ref: '#/components/schemas/RglLayoutItem' } }
        md: { type: array, items: { $ref: '#/components/schemas/RglLayoutItem' } }
        lg: { type: array, items: { $ref: '#/components/schemas/RglLayoutItem' } }
        xl: { type: array, items: { $ref: '#/components/schemas/RglLayoutItem' } }

    GlobalTabsOrderItem:
      type: object
      required: [systemId, visible]
      additionalProperties: false
      properties:
        systemId: { type: string, minLength: 1 }
        visible: { type: boolean }

    DashboardSettings:
      type: object
      required:
        - user_id
        - widget_order
        - widget_visibility
        - widget_sizes
        - locked_fields
        - default_date_range
        - heatmap_default_x_axis
        - globaltabs_order
        - updated_at
      properties:
        user_id:
          type: string
          format: uuid
          nullable: true
        widget_order:
          type: array
          items: { type: string }
        widget_visibility:
          type: object
          additionalProperties: { type: boolean }
        widget_sizes:
          $ref: '#/components/schemas/RglLayouts'
        locked_fields:
          type: array
          items: { type: string }
        default_date_range:
          type: string
          enum: ['1m', '3m', '1y', 'all', 'custom']
        heatmap_default_x_axis:
          type: string
          enum: ['status', 'priority', 'tag']
        globaltabs_order:
          nullable: true
          type: array
          items: { $ref: '#/components/schemas/GlobalTabsOrderItem' }
        updated_at:
          type: string
          format: date-time

    DashboardSettingsUpdate:
      type: object
      additionalProperties: false
      properties:
        widget_order:
          type: array
          items: { type: string }
        widget_visibility:
          type: object
          additionalProperties: { type: boolean }
        widget_sizes:
          $ref: '#/components/schemas/RglLayouts'
        locked_fields:
          type: array
          items: { type: string }
        default_date_range:
          type: string
          enum: ['1m', '3m', '1y', 'all', 'custom']
        heatmap_default_x_axis:
          type: string
          enum: ['status', 'priority', 'tag']
        globaltabs_order:
          nullable: true
          type: array
          items: { $ref: '#/components/schemas/GlobalTabsOrderItem' }

    DistributionItem:
      type: object
      required: [label, count]
      properties:
        label:
          type: string
        count:
          type: integer
        percentage:
          type: number

    WeeklyTrendItem:
      type: object
      required: [week, count]
      properties:
        week:
          type: string
        count:
          type: integer

    TagStatItem:
      type: object
      required: [tag_id, name, count]
      properties:
        tag_id:
          type: string
          format: uuid
        name:
          type: string
        count:
          type: integer

    SystemStatItem:
      type: object
      required: [system_id, name, count]
      properties:
        system_id:
          type: string
          format: uuid
        name:
          type: string
        count:
          type: integer

    MasterRefreshResult:
      type: object
      required: [swapped, loaded_at, sources]
      properties:
        swapped:
          type: boolean
        loaded_at:
          type: string
          format: date-time
        sources:
          type: object
          required: [equipment, db]
          properties:
            equipment:
              type: object
              required: [loaded_at]
              properties:
                loaded_at: { type: string, format: date-time }
            db:
              type: object
              required: [loaded_at]
              properties:
                loaded_at: { type: string, format: date-time }

    MasterStatusSourceEntry:
      type: object
      required: [loaded_at]
      properties:
        loaded_at:
          type: string
          format: date-time
        kept_loaded_at:
          type: string
          format: date-time

    MasterStatus:
      type: object
      required: [loaded_at, cooldown_until, sources]
      properties:
        loaded_at:
          type: string
          format: date-time
          nullable: true
        cooldown_until:
          type: string
          format: date-time
          nullable: true
        sources:
          type: object
          required: [equipment, db, program]
          properties:
            equipment: { $ref: '#/components/schemas/MasterStatusSourceEntry' }
            db:        { $ref: '#/components/schemas/MasterStatusSourceEntry' }
            program:   { $ref: '#/components/schemas/MasterStatusSourceEntry' }

    TagMasterItem:
      # NOTE: `merged_into_id` removed per Resolution α (PR #251) — merge = hard-delete.
      type: object
      required: [id, name, slug, kind, is_external, usage_count, rule_ref_count, created_at]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        kind:
          type: string
          enum: [general, menu]
        is_external: { type: boolean }
        usage_count: { type: integer, minimum: 0 }
        rule_ref_count: { type: integer, minimum: 0 }
        created_at: { type: string, format: date-time }

    TagMasterListResponse:
      type: object
      required: [rows, page, per_page, total]
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/TagMasterItem' }
        page: { type: integer, minimum: 1 }
        per_page: { type: integer, minimum: 1 }
        total: { type: integer, minimum: 0 }

    TagMasterCreate:
      type: object
      required: [name, kind]
      properties:
        name: { type: string, minLength: 1, maxLength: 80 }
        kind:
          type: string
          enum: [general, menu]

    TagMasterPatch:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 80 }

    TagMasterMergeInput:
      type: object
      required: [targetId]
      properties:
        targetId: { type: string, format: uuid }

    TagExternalToggle:
      type: object
      required: [is_external]
      properties:
        is_external: { type: boolean }

    TagRuleSuspendInput:
      type: object
      required: [suspended_until]
      properties:
        suspended_until:
          type: string
          format: date-time
          nullable: true

    TrashListItem:
      type: object
      required: [id, issue_code, title, status, system_id, menu_id, deleted_by, deleted_at]
      properties:
        id: { type: string, format: uuid }
        issue_code: { type: string }
        title: { type: string }
        status: { $ref: '#/components/schemas/VocStatus' }
        system_id: { type: string, format: uuid }
        menu_id: { type: string, format: uuid }
        deleted_by: { type: string, format: uuid, nullable: true }
        deleted_at: { type: string, format: date-time }

    TrashListResponse:
      type: object
      required: [rows, page, per_page, total]
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/TrashListItem' }
        page: { type: integer, minimum: 1 }
        per_page: { type: integer, minimum: 1 }
        total: { type: integer, minimum: 0 }

    VocRestoreLogEntry:
      type: object
      required: [id, voc_id, action, actor_id, before_deleted_at, before_deleted_by, created_at]
      properties:
        id: { type: string, format: uuid }
        voc_id: { type: string, format: uuid }
        action:
          type: string
          enum: [restore, hard_delete]
        actor_id: { type: string, format: uuid }
        before_deleted_at: { type: string, format: date-time, nullable: true }
        before_deleted_by: { type: string, format: uuid, nullable: true }
        created_at: { type: string, format: date-time }

    VocRestoreResponse:
      type: object
      required: [voc_id, restored_at, audit]
      properties:
        voc_id: { type: string, format: uuid }
        restored_at: { type: string, format: date-time }
        audit: { $ref: '#/components/schemas/VocRestoreLogEntry' }

    AdminUserItem:
      type: object
      required: [id, ad_username, display_name, email, role, is_active, created_at]
      properties:
        id: { type: string, format: uuid }
        ad_username: { type: string }
        display_name: { type: string }
        email: { type: string, format: email }
        role: { $ref: '#/components/schemas/UserRole' }
        is_active: { type: boolean }
        created_at: { type: string, format: date-time }

    AdminUserListResponse:
      type: object
      required: [rows, page, per_page, total]
      properties:
        rows:
          type: array
          items: { $ref: '#/components/schemas/AdminUserItem' }
        page: { type: integer, minimum: 1 }
        per_page: { type: integer, minimum: 1 }
        total: { type: integer, minimum: 0 }

    AdminUserPatch:
      # At least one of `role` / `is_active` required (mirrors zod .refine()).
      type: object
      anyOf:
        - required: [role]
        - required: [is_active]
      properties:
        role: { $ref: '#/components/schemas/UserRole' }
        is_active: { type: boolean }
        reason: { type: string, maxLength: 500 }

    UserRoleLogEntry:
      type: object
      required: [id, user_id, changed_by, old_role, new_role, old_active, new_active, reason, created_at]
      properties:
        id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        changed_by: { type: string, format: uuid }
        old_role:
          type: string
          enum: [user, dev, manager, admin]
          nullable: true
        new_role:
          type: string
          enum: [user, dev, manager, admin]
          nullable: true
        old_active: { type: boolean, nullable: true }
        new_active: { type: boolean, nullable: true }
        reason: { type: string, nullable: true }
        created_at: { type: string, format: date-time }

    VocPayloadHistory:
      type: object
      required: [id, voc_id, payload, submitted_by, submitted_at, is_current]
      properties:
        id:
          type: string
          format: uuid
        voc_id:
          type: string
          format: uuid
        payload:
          type: object
          additionalProperties: true
        submitted_by:
          type: string
          format: uuid
        submitted_at:
          type: string
          format: date-time
        final_state:
          type: string
          nullable: true
        is_current:
          type: boolean

    Notice:
      type: object
      required:
        [id, title, body, level, is_popup, is_visible, author_id, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        body: { type: string }
        level: { type: string, enum: [normal, important, urgent] }
        is_popup: { type: boolean }
        is_visible: { type: boolean }
        visible_from: { type: string, format: date-time, nullable: true }
        visible_to: { type: string, format: date-time, nullable: true }
        author_id: { type: string, format: uuid }
        deleted_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    NoticeInput:
      type: object
      required: [title, body]
      properties:
        title: { type: string }
        body: { type: string }
        level: { type: string, enum: [normal, important, urgent] }
        is_popup: { type: boolean }
        is_visible: { type: boolean }
        visible_from: { type: string, format: date-time, nullable: true }
        visible_to: { type: string, format: date-time, nullable: true }

    FaqCategory:
      type: object
      required: [id, name, slug, sort_order, is_archived]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        sort_order: { type: integer }
        is_archived: { type: boolean }

    FaqCategoryInput:
      type: object
      required: [name, slug]
      properties:
        name: { type: string }
        slug: { type: string }
        sort_order: { type: integer }
        is_archived: { type: boolean }

    Faq:
      type: object
      required:
        [id, question, answer, category_id, is_visible, sort_order, author_id, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        question: { type: string }
        answer: { type: string }
        category_id: { type: string, format: uuid }
        is_visible: { type: boolean }
        sort_order: { type: integer }
        author_id: { type: string, format: uuid }
        deleted_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    FaqInput:
      type: object
      required: [question, answer, category_id]
      properties:
        question: { type: string }
        answer: { type: string }
        category_id: { type: string, format: uuid }
        is_visible: { type: boolean }
        sort_order: { type: integer }
