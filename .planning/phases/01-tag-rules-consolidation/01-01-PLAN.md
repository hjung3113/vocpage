---
phase: 01-tag-rules-consolidation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/check-fixture-seed-parity.ts
  - shared/fixtures/admin-tag-rule.fixtures.ts
  - backend/src/__tests__/migration-024.test.ts
  - frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts
  - .planning/phases/01-tag-rules-consolidation/01-MATCHER-AUDIT.md
autonomous: true
requirements: [REQ-admin-pages-wave]

threat_model:
  trust_boundaries:
    - "Build tooling → fixture+migration parity (catches schema/fixture drift before runtime)"
  threats:
    - id: T-01-01
      category: T
      component: "scripts/check-fixture-seed-parity.ts"
      disposition: mitigate
      mitigation: "Generalized triples + ALTER-TABLE parsing — schema additions in migrations 014/024 are now reflected in parity check; coverage sustained via Wave 0 fixtures."

must_haves:
  truths:
    - "Failing test exists for mig 024 (RED) — `migration-024.test.ts` runs but fails because mig file does not exist yet."
    - "Failing test exists for D-11 optimistic update — `optimistic.test.ts` runs but fails because hooks do not exist yet."
    - "Parity script accepts `tag_rules` triple — extending vocs-only hardcode."
    - "Auto-tag matcher consumers of `tag_rules.pattern` are enumerated in `01-MATCHER-AUDIT.md` (proves how many BE files Wave 1 must reconcile when `pattern` is dropped)."
  artifacts:
    - path: "backend/src/__tests__/migration-024.test.ts"
      provides: "pg-mem up/down round-trip for mig 024 (NEW)"
    - path: "frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts"
      provides: "TanStack v5 onMutate/onError/onSettled rollback contract"
    - path: "scripts/check-fixture-seed-parity.ts"
      provides: "triples-based parity (vocs + tag_rules + future)"
    - path: "shared/fixtures/admin-tag-rule.fixtures.ts"
      provides: "tag_rules fixture rows aligned to mig 024 schema"
    - path: ".planning/phases/01-tag-rules-consolidation/01-MATCHER-AUDIT.md"
      provides: "list of files reading tag_rules.pattern that must change in Wave 1"
  key_links:
    - from: "scripts/check-fixture-seed-parity.ts"
      to: "shared/fixtures/admin-tag-rule.fixtures.ts"
      via: "TARGETS triple (table=tag_rules, migration, fixture, sampleKey)"
      pattern: "tag_rules.*admin-tag-rule.fixtures"
---

<objective>
Wave 0 scaffolding: lay every failing test, generalize the parity tool, and audit the codebase for `tag_rules.pattern` consumers BEFORE Waves 1+ touch schema or contracts.

Purpose: TDD for irreversible surface (DB migration + contracts) requires RED tests first. Auto-tag matcher audit (OQ-R3) is BLOCKING for Wave 1 because Plan 02 must reconcile every reader of `pattern` to `keywords[]` in the same commit window.

Output: 4 new files (3 tests + 1 audit doc), 1 generalized script, 1 fixture file. All FE+BE quick test runs still preserve the BE 593+ / FE 691+ baselines (new tests fail, but they fail in `describe.skip`-equivalent guards or `xtest` so the suite passes — see acceptance criteria).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tag-rules-consolidation/01-CONTEXT.md
@.planning/phases/01-tag-rules-consolidation/01-RESEARCH.md
@.planning/phases/01-tag-rules-consolidation/01-PATTERNS.md
@.planning/phases/01-tag-rules-consolidation/01-VALIDATION.md
@CLAUDE.md
@backend/CLAUDE.md
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.01.1: Audit auto-tag matcher consumers of tag_rules.pattern</name>
  <files>.planning/phases/01-tag-rules-consolidation/01-MATCHER-AUDIT.md</files>
  <read_first>
    - .planning/phases/01-tag-rules-consolidation/01-RESEARCH.md (Pitfall 7 + OQ-R3)
    - .planning/phases/01-tag-rules-consolidation/01-PATTERNS.md (pitfall ↔ pattern cross-reference)
  </read_first>
  <action>
    Run two enumeration commands and write the output (file:line + 1-line description per hit) into `01-MATCHER-AUDIT.md`:

    1. `rg -n "tag_rules" backend/src/services/`
    2. `rg -n "\.pattern" backend/src/services/ backend/src/repository/ | rg -i "tag|rule"`

    For each hit, classify as:
    - **Reader of `pattern`** — must change in Wave 1 (Plan 02 reconcile to `keywords[]`).
    - **Reader of other tag_rules columns** — unaffected.
    - **Trash restore re-run** (`backend/src/repository/trash.ts:91, 145–160`) — D-14 confirms REST-rename safe; verify no `.pattern` SELECT.

    Audit doc structure:
    ```
    # Mig 024 — `tag_rules.pattern` consumer audit (Wave 0)

    ## Summary
    {N} files read `tag_rules`; {M} read `.pattern` specifically; {K} of those must reconcile in Plan 02.

    ## Files that read `tag_rules.pattern` (MUST change in Plan 02)
    - `<path>:<line>` — <one-line description of usage>
    ...

    ## Files that read tag_rules but NOT pattern (no Plan 02 change)
    - `<path>:<line>` — <description>
    ...

    ## Verification
    Run `rg -n "\\.pattern" backend/src/ | rg -i "tag|rule"` — every hit traced above.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/01-tag-rules-consolidation/01-MATCHER-AUDIT.md &amp;&amp; grep -c "MUST change in Plan 02" .planning/phases/01-tag-rules-consolidation/01-MATCHER-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/01-tag-rules-consolidation/01-MATCHER-AUDIT.md`.
    - Contains heading `## Files that read \`tag_rules.pattern\` (MUST change in Plan 02)` (grep: `grep -c "MUST change in Plan 02" 01-MATCHER-AUDIT.md` returns ≥1).
    - Every file listed in the MUST-change section is reachable: `while read f; do test -f "$f" || echo MISSING; done < <(grep -oE "^\- \`[^:]+" 01-MATCHER-AUDIT.md | sed 's/^\- `//')` returns no MISSING lines.
    - Trash restore mention: grep `backend/src/repository/trash.ts` in the audit doc — must appear with explicit `does NOT read .pattern` or `MUST change` classification (no silent omission).
  </acceptance_criteria>
  <done>Audit doc written; planner Plan 02 will read it to enumerate exact reconciliation targets.</done>
</task>

<task type="auto">
  <name>Task 1.01.2: Generalize check-fixture-seed-parity.ts to triples + add tag_rules target</name>
  <files>scripts/check-fixture-seed-parity.ts, shared/fixtures/admin-tag-rule.fixtures.ts</files>
  <read_first>
    - scripts/check-fixture-seed-parity.ts (current vocs-only hardcode at lines 21–53)
    - shared/fixtures/admin-tag.fixtures.ts (existing TAG_IDS / TAG_RULE_IDS declarations)
    - backend/migrations/004_tags.sql (base CREATE TABLE tag_rules)
    - backend/migrations/014_tag_master_ops.sql (ALTER TABLE adds suspended_until)
    - .planning/phases/01-tag-rules-consolidation/01-PATTERNS.md (§scripts/check-fixture-seed-parity.ts excerpt)
  </read_first>
  <action>
    1. Refactor `scripts/check-fixture-seed-parity.ts` to accept TARGETS triples:
       ```typescript
       const TARGETS: Array<{
         table: string;
         migrations: string[];   // base CREATE + each ALTER, in apply order
         fixture: string;
         sampleKey: string;
         triggerCols?: Set<string>;
       }> = [
         { table: 'vocs',     migrations: ['backend/migrations/003_vocs.sql'], fixture: 'shared/fixtures/voc.fixtures.ts', sampleKey: 'VOC_FIXTURES', triggerCols: new Set(['sequence_no','issue_code']) },
         { table: 'tag_rules', migrations: ['backend/migrations/004_tags.sql','backend/migrations/014_tag_master_ops.sql'], fixture: 'shared/fixtures/admin-tag-rule.fixtures.ts', sampleKey: 'ADMIN_TAG_RULE_FIXTURES' },
       ];
       ```
       NOTE: Plan 02 will append `'backend/migrations/024_tag_rules_created_by.sql'` to the tag_rules migrations array. Do NOT add 024 here yet (it doesn't exist).

    2. Extend `parseDbColumns(...)` to merge `CREATE TABLE <table>` columns with subsequent `ALTER TABLE <table> ADD COLUMN <name> <type>` clauses across the migrations array. `DROP COLUMN` removes from the merged set.

    3. The script must iterate TARGETS, run all checks per target, accumulate errors, and exit non-zero if any.

    4. Create `shared/fixtures/admin-tag-rule.fixtures.ts` with at least 2 rows referencing `TAG_IDS` from `shared/fixtures/admin-tag.fixtures.ts`. Schema for THIS wave (mig 014 only — pre mig 024):
       - id (uuid from `TAG_RULE_IDS`)
       - tag_id (uuid)
       - kind: 'general'
       - pattern: '{"keywords":["bug","fix"]}' (legacy text — Plan 02 will re-shape this fixture when keywords[] column lands)
       - is_active: true
       - suspended_until: null
       - created_at: ISO string

       Export as `ADMIN_TAG_RULE_FIXTURES`. Keep type-shape minimal (Plan 03 will re-type against the new zod TagRule schema).

    5. Run `npx tsx scripts/check-fixture-seed-parity.ts` — must currently PASS (mig 024 not yet in TARGETS). The vocs target must continue to pass; the tag_rules target validates against pre-024 schema.
  </action>
  <verify>
    <automated>npx tsx scripts/check-fixture-seed-parity.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TARGETS" scripts/check-fixture-seed-parity.ts` ≥ 1 (constant introduced).
    - `grep -c "ALTER TABLE" scripts/check-fixture-seed-parity.ts` ≥ 1 (ALTER parsing implemented).
    - `grep -c "ADMIN_TAG_RULE_FIXTURES" shared/fixtures/admin-tag-rule.fixtures.ts` ≥ 1.
    - `grep -c "TAG_IDS\|TAG_RULE_IDS" shared/fixtures/admin-tag-rule.fixtures.ts` ≥ 1 (uses shared fixture IDs).
    - `npx tsx scripts/check-fixture-seed-parity.ts` exits 0.
    - vocs parity is not regressed: grep `vocs` in script still inside TARGETS array.
  </acceptance_criteria>
  <done>Triples-based parity script ships, vocs target green, tag_rules target green against pre-mig-024 schema, fixture file created.</done>
</task>

<task type="auto">
  <name>Task 1.01.3: Write failing tests for mig 024 + D-11 optimistic update (RED phase)</name>
  <files>backend/src/__tests__/migration-024.test.ts, frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts</files>
  <read_first>
    - backend/src/__tests__/migration-014.test.ts (full file — exact analog)
    - frontend/src/features/admin/tag-master/api/tag-master.api.ts (existing useSuspendTagRule mutation shape)
    - .planning/phases/01-tag-rules-consolidation/01-PATTERNS.md (§migration-024.test.ts boot + parse pattern, §optimistic.test.ts requirements)
    - .planning/phases/01-tag-rules-consolidation/01-RESEARCH.md (OQ-R1 schema decision: keywords text[] + match_mode text + drop pattern + add created_by)
  </read_first>
  <action>
    **A. `backend/src/__tests__/migration-024.test.ts`** — mirror `migration-014.test.ts` structure.

    - `MIGRATIONS_DIR` resolves to `../../migrations`.
    - `BASE_FILES = ['004_tags.sql', '014_tag_master_ops.sql']` (mig 024 inherits both).
    - Add `USERS_STUB = 'CREATE TABLE users (id uuid PRIMARY KEY);'` alongside `VOCS_STUB` so `created_by uuid REFERENCES users(id)` resolves.
    - Reuse `stripUnsupported` (drop CREATE EXTENSION, embedding vector, swap uuid_generate_v4 → gen_random_uuid).
    - Reuse `readMigration` (regex on `-- Up Migration` / `-- Down Migration`).
    - Reuse `describeColumns` helper.

    Tests (5):
    1. `up adds keywords text[] NOT NULL DEFAULT '{}' to tag_rules` — assert column present with array type.
    2. `up adds match_mode text NOT NULL DEFAULT 'keyword' to tag_rules` — assert column + default.
    3. `up drops pattern column from tag_rules` — assert column absent.
    4. `up adds created_by uuid NULL REFERENCES users(id) to tag_rules` — assert column + nullable.
    5. `down restores pattern column shape and drops keywords/match_mode/created_by` — assert pre-up shape after up→down.

    Acknowledge mig file does not exist yet. Use `try { readMigration('024_tag_rules_created_by.sql') } catch { test.skip(...) }` so the suite still passes the BE 593+ baseline. Plan 02 removes the skip.

    Add a top-level guard test that DOES run and fails if the migration file appears: actually, prefer `test.todo('mig 024 written by Plan 02')` for each test when file absent. Use `fs.existsSync(...)` to switch between todo and live tests.

    **B. `frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts`** — D-11 contract.

    Pattern: Vitest + `@tanstack/react-query` `QueryClient` + `renderHook` from `@testing-library/react`. Mock `apiPost` / `apiDelete` from `@shared/api/client`.

    Tests (4):
    1. `useCreateTagRule onMutate increments rule_ref_count for the targeted tag in admin tags cache by +1`.
    2. `useCreateTagRule onError restores prev cache snapshot`.
    3. `useCreateTagRule onSettled invalidates ['admin','tags'] and ['admin','tags',tagId,'rules']`.
    4. `useCreateTagRule concurrent double-fire — both onMutate paths cancelQueries first; onSettled invalidate is the ground truth (Pitfall 5)`.

    Mark each test `test.todo('Plan 05 implements useCreateTagRule')` when the hook does not yet export. Use a `try { import { useCreateTagRule } } catch` shim so file lints clean.

    Both files MUST be syntactically valid (typecheck green); they MUST NOT regress the FE Vitest 691+ / BE Jest 593+ baseline.
  </action>
  <verify>
    <automated>npm run typecheck -w backend &amp;&amp; npm run typecheck -w frontend &amp;&amp; npm run test -w backend -- --testPathPattern=migration-024 | tail -20 &amp;&amp; npm run test -w frontend -- --run frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/src/__tests__/migration-024.test.ts` returns true.
    - `test -f frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts` returns true.
    - `grep -c "describe.*migration 024" backend/src/__tests__/migration-024.test.ts` ≥ 1.
    - `grep -cE "test\.todo|test\.skip|describe\.skip" backend/src/__tests__/migration-024.test.ts` ≥ 1 (RED guard until mig file exists).
    - `grep -c "useCreateTagRule" frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts` ≥ 1.
    - `grep -cE "test\.todo|test\.skip" frontend/src/features/admin/tag-master/api/__tests__/optimistic.test.ts` ≥ 1.
    - BE Jest baseline preserved: `npm run test -w backend | tail -5` reports passing tests ≥ 593.
    - FE Vitest baseline preserved: `npm run test -w frontend -- --run | tail -5` reports passing tests ≥ 691.
  </acceptance_criteria>
  <done>RED tests staged with todo guards; baselines intact; Plans 02 / 05 will flip todos to live.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/check-fixture-seed-parity.ts` — exit 0 (vocs + tag_rules pre-024 green).
- `npm run test -w backend | tail -5` — baseline ≥ 593 passes preserved.
- `npm run test -w frontend -- --run | tail -5` — baseline ≥ 691 passes preserved.
- `01-MATCHER-AUDIT.md` enumerates every `tag_rules.pattern` reader.
</verification>

<success_criteria>
- Wave 0 scaffolds in place; Plan 02 has a complete consumer list and a failing migration test to drive RED→GREEN.
- Plan 05 has a failing optimistic test to drive D-11 implementation.
- Parity script generalized; tag_rules now in TARGETS (Plan 02 will append mig 024 to the migrations array).
</success_criteria>

<output>
After completion, create `.planning/phases/01-tag-rules-consolidation/01-01-SUMMARY.md` with:
- Files created
- Audit headcount: how many files Plan 02 must reconcile
- Any surprises (e.g., trash.ts reads .pattern after all → escalate to D-14 review)
</output>
