---
phase: 01-tag-rules-consolidation
plan: 07
type: execute
wave: 3
depends_on: [05, 06]
files_modified:
  - frontend/src/features/admin/tag-master/ui/TagMasterTable.tsx
  - frontend/src/features/admin/tag-master/ui/TagRulesFlatTable.tsx
  - frontend/src/pages/admin/tags.tsx
  - frontend/src/pages/admin/__tests__/AdminTagsPage.test.tsx
autonomous: true
requirements: [REQ-admin-pages-wave]

threat_model:
  trust_boundaries:
    - "URL query state → server q parameter — must not enable XSS via reflected search input"
  threats:
    - id: T-01-16
      category: T
      component: "?q= URL state"
      disposition: mitigate
      mitigation: "useSearchParams encodes; React renders q via JSX (auto-escapes). Search input bound to controlled state; debounced 250ms before firing query."

must_haves:
  truths:
    - "TagMasterTable rows show 규칙 N건 column between 사용 VOC 수 and 작업; clicking opens TagRulesManagerModal."
    - "/admin/tags top toolbar shows 태그 / 전체 규칙 tabs; default view=tags."
    - "Selecting 전체 규칙 tab renders flat table with q search input; q debounce 250ms; URL updates ?view=rules&q=<text>."
    - "Empty / loading / error states render per UI-SPEC.md §Copywriting Contract for view=rules."
    - "Direct URL ?view=rules&q=foo on page load renders that state without flicker."
  artifacts:
    - path: "frontend/src/features/admin/tag-master/ui/TagMasterTable.tsx"
      provides: "Existing table extended with 규칙 N건 column"
    - path: "frontend/src/features/admin/tag-master/ui/TagRulesFlatTable.tsx"
      provides: "Flat all-rules table with q search"
    - path: "frontend/src/pages/admin/tags.tsx"
      provides: "Page with view-mode tabs + URL state + modal mount"
  key_links:
    - from: "frontend/src/pages/admin/tags.tsx"
      to: "useSearchParams"
      via: "view + q URL state"
      pattern: "useSearchParams"
    - from: "TagMasterTable 규칙 N건 cell"
      to: "TagRulesManagerModal"
      via: "onClick → setRulesModalTag(tag)"
      pattern: "TagRulesManagerModal"
---

<objective>
Wire the Modal into TagMasterTable's row badge, build the flat all-rules table for `view=rules`, and add view-mode tabs + URL state to the `/admin/tags` page. Extend the page integration test to cover the full tab + search + modal-open journey.

Purpose: Realizes SC-1 (badge → modal entry) + SC-4 (전체 규칙 보기 search) + D-03 / D-04 (URL state). Last FE plan before spec sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tag-rules-consolidation/01-UI-SPEC.md
@.planning/phases/01-tag-rules-consolidation/01-CONTEXT.md
@.planning/phases/01-tag-rules-consolidation/01-PATTERNS.md
@.planning/phases/01-tag-rules-consolidation/01-06-SUMMARY.md
@frontend/src/features/admin/tag-master/ui/TagMasterTable.tsx
@frontend/src/pages/admin/tags.tsx
@frontend/src/pages/admin/__tests__/AdminTagsPage.test.tsx
@frontend/src/features/voc/list/model/useVocFilterUrlState.ts
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.07.1: Add 규칙 N건 column to TagMasterTable + build TagRulesFlatTable</name>
  <files>frontend/src/features/admin/tag-master/ui/TagMasterTable.tsx, frontend/src/features/admin/tag-master/ui/TagRulesFlatTable.tsx</files>
  <read_first>
    - frontend/src/features/admin/tag-master/ui/TagMasterTable.tsx (full file — current column shape)
    - frontend/src/shared/ui/badge.tsx (OutlineChip / SolidChip primitives — confirm naming)
    - frontend/src/shared/ui/input.tsx (search input)
    - .planning/phases/01-tag-rules-consolidation/01-UI-SPEC.md (§Layout & Interaction Contract — 규칙 N건 row badge states; §view=rules flat table; §Search input recipe; §Copywriting Contract empty states)
    - .planning/phases/01-tag-rules-consolidation/01-CONTEXT.md (D-03)
    - frontend/src/features/admin/tag-master/api/tag-master.api.ts (Plan 05 hooks — `useAdminTagRules`; if a cross-tag list is needed, decide here)
  </read_first>
  <action>
    1. **TagMasterTable.tsx** — add `규칙 N건` column:
       - Insert column header between `사용 VOC 수` and `작업`.
       - Cell renders per UI-SPEC §Row badge states:
         - N=0: TextMark `규칙 없음` styled with `--text-quaternary`. Wrapped in `<button>` for a11y; clicking opens modal in empty state.
         - N≥1: OutlineChip `규칙 {N}건` (`--brand-bg`/`--accent`/`--brand-border`).
       - aria-label: `${tag.name}의 규칙 ${N}건 관리`.
       - State managed by parent (page): the table accepts `onOpenRules: (tag) => void` prop. Add the prop; refactor cell click to `onClick={() => onOpenRules(tag)}`.
       - Hover/focus states inherit from existing button styling.

    2. **TagRulesFlatTable.tsx** — NEW component:
       - Props: `{ q: string; onClearSearch: () => void }` (parent owns URL state).
       - Internally call a list endpoint that returns ALL tag rules (cross-tag). Two options:
         a. Reuse `useAdminTagRules` per-tag for each tag (N+1 — bad).
         b. Add a new `useAllTagRules({ q })` hook backed by a NEW BE list endpoint `GET /api/admin/rules?q=` — but that's out of phase scope (no such endpoint).
         c. **PICKED:** Iterate the existing `useAdminTags()` to get the tag list; for each tag use `useAdminTagRules(tagId, { q })`. Use a single combined query: send N parallel queries via `useQueries` from TanStack v5; flatten responses into a single array.
       - Columns: 키워드 | 태그 | 매칭 방식 | 상태 | 작성자 | 작업.
       - 키워드 cell: render chips inline (max 3 + `+N`).
       - 태그 cell: render parent tag name as a `<button>` that calls a `onJumpToTag(tagId: string)` prop — the page uses this to flip `?view=tags` and (best-effort) scroll to the tag row.
       - 매칭 방식: `키워드`.
       - 상태: 활성 / 일시중지됨.
       - 작성자: `created_by_name` || `—`.
       - 작업: same DropdownMenu pattern from Modal (수정/일시중지/재개/삭제) gated by isAdmin. 수정 here opens TagRulesManagerModal scoped to that tag (parent owns modal state) — pass `onEditRule(tagId, ruleId)` prop up.
       - Search empty state: when `q !== ''` and merged rows length is 0, render EmptyState with `검색 결과가 없습니다` + body `다른 키워드로 다시 검색하거나 검색어를 비워 전체 규칙을 확인하세요`.
       - Total empty: when `q === ''` and merged rows length is 0, render `등록된 규칙이 없습니다` + body `태그 탭에서 태그별로 규칙을 추가할 수 있습니다`.
       - Loading: 3 skeleton rows.

       NOTE on q debouncing: q is provided by the parent — debouncing happens in the parent's URL-state setter (Task 1.07.2). This component receives the already-debounced q.

    3. Run `npm run typecheck -w frontend`. No tests for these two files yet — coverage comes via the page integration test in Task 1.07.2.
  </action>
  <verify>
    <automated>npm run typecheck -w frontend</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "규칙 N건|규칙 \\{|rule_ref_count" frontend/src/features/admin/tag-master/ui/TagMasterTable.tsx` ≥ 1.
    - `grep -c "onOpenRules" frontend/src/features/admin/tag-master/ui/TagMasterTable.tsx` ≥ 1 (prop wired).
    - `test -f frontend/src/features/admin/tag-master/ui/TagRulesFlatTable.tsx`.
    - `grep -c "export function TagRulesFlatTable" frontend/src/features/admin/tag-master/ui/TagRulesFlatTable.tsx` ≥ 1.
    - `grep -cE "검색 결과가 없습니다|등록된 규칙이 없습니다" frontend/src/features/admin/tag-master/ui/TagRulesFlatTable.tsx` ≥ 2.
    - `grep -c "useQueries\\|useAdminTagRules" frontend/src/features/admin/tag-master/ui/TagRulesFlatTable.tsx` ≥ 1.
    - typecheck exits 0.
  </acceptance_criteria>
  <done>Both surfaces ready; page wires them in next task.</done>
</task>

<task type="auto">
  <name>Task 1.07.2: Wire view-mode tabs + URL state + modal mount on /admin/tags page + extend test</name>
  <files>frontend/src/pages/admin/tags.tsx, frontend/src/pages/admin/__tests__/AdminTagsPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/admin/tags.tsx (full 22 lines — current thin wrapper)
    - frontend/src/pages/admin/__tests__/AdminTagsPage.test.tsx (full file)
    - frontend/src/features/voc/list/model/useVocFilterUrlState.ts (URL state pattern at lines 1-60)
    - frontend/src/shared/ui/tabs.tsx (Radix tabs primitive)
    - .planning/phases/01-tag-rules-consolidation/01-UI-SPEC.md (§/admin/tags page anatomy, §View-mode tabs recipe, §Search input recipe — 250ms debounce)
    - .planning/phases/01-tag-rules-consolidation/01-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
    1. **Refactor `frontend/src/pages/admin/tags.tsx`**:
       - Read URL params via `useSearchParams`:
         ```tsx
         const [params, setParams] = useSearchParams();
         const view = params.get('view') === 'rules' ? 'rules' : 'tags';
         const qParam = params.get('q') ?? '';
         ```
       - Local debounced q state for the input (250ms): `const [qDraft, setQDraft] = useState(qParam); useEffect(() => { const id = setTimeout(() => { setParams((p) => { if (qDraft) p.set('q', qDraft); else p.delete('q'); return p; }, { replace: true }); }, 250); return () => clearTimeout(id); }, [qDraft])`.
       - View setter: when user picks 태그, also clear q.
       - Modal state: `const [rulesModalTag, setRulesModalTag] = useState<TagMasterItem | null>(null)`.
       - Layout per UI-SPEC §Layout:
         - Toolbar 44px row: left = `<Tabs value={view} onValueChange={...}>` with two tabs `태그` / `전체 규칙`; right = search input only when `view === 'rules'`.
         - Body: `view === 'tags' ? <TagMasterTable onOpenRules={setRulesModalTag} ... /> : <TagRulesFlatTable q={qParam} onJumpToTag={(id) => { setParams((p) => { p.set('view','tags'); p.delete('q'); return p; }); /* TODO scroll to row id - best-effort */ }} onEditRule={(tagId) => /* setRulesModalTag with tag */} />`.
         - Modal: `{rulesModalTag && <TagRulesManagerModal tag={rulesModalTag} onClose={() => setRulesModalTag(null)} />}`.
       - Keep existing role guard at top (lines 11-15) unchanged.
       - Korean labels: tab `태그` / `전체 규칙`; search placeholder `키워드 또는 태그명으로 검색`.

    2. **Extend `AdminTagsPage.test.tsx`** with new cases (preserve existing tests):
       - Default render: URL `/admin/tags` → 태그 tab active; TagMasterTable renders with 규칙 N건 column.
       - Click 전체 규칙 tab → URL becomes `/admin/tags?view=rules` (assertion via `window.location.search`); TagRulesFlatTable renders.
       - Type "bug" in search input → after 250ms, URL becomes `?view=rules&q=bug`; flat table re-renders with filtered rows.
       - Click 규칙 N건 badge on a tag row → modal opens (header contains tag name).
       - Optimistic SC-2: in modal, type keyword + click + 규칙 추가 → parent table 규칙 N건 cell shows `N+1` BEFORE MSW handler resolves (use a slight delay in MSW). After settle, cell still shows N+1.
       - Direct deep link: render with initial URL `/admin/tags?view=rules&q=foo` → flat table renders immediately with q=foo applied.
       - Empty result for `?view=rules&q=zzz` → empty state copy `검색 결과가 없습니다`.

       Use `MemoryRouter` initial entries for deep-link cases. Use `vi.useFakeTimers` for the 250ms debounce assertion (advance timers before checking URL).

    3. Run `npm run typecheck -w frontend && npm run test -w frontend -- --run frontend/src/pages/admin/__tests__/AdminTagsPage.test.tsx | tail -30`. All passing.

    4. Run full FE quick batch: `npm run typecheck -w frontend && npm run test -w frontend -- --run | tail -20`. Baseline ≥ 691 + Phase 1 net additions; 0 failures.

    5. Run `npm run lint -w frontend` once (per project convention before first commit of FE changes).
  </action>
  <verify>
    <automated>npm run typecheck -w frontend &amp;&amp; npm run test -w frontend -- --run frontend/src/pages/admin/__tests__/AdminTagsPage.test.tsx | tail -30 &amp;&amp; npm run lint -w frontend | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useSearchParams" frontend/src/pages/admin/tags.tsx` ≥ 1.
    - `grep -cE "view.*rules|view=rules" frontend/src/pages/admin/tags.tsx` ≥ 1.
    - `grep -c "TagRulesManagerModal" frontend/src/pages/admin/tags.tsx` ≥ 1.
    - `grep -c "TagRulesFlatTable" frontend/src/pages/admin/tags.tsx` ≥ 1.
    - `grep -c "250" frontend/src/pages/admin/tags.tsx` ≥ 1 (debounce timeout literal).
    - `grep -cE "전체 규칙|키워드 또는 태그명으로 검색" frontend/src/pages/admin/tags.tsx` ≥ 2.
    - AdminTagsPage.test.tsx test count: ≥ baseline + 6 new (verify by counting `it(` / `test(` matches before vs after).
    - Full FE Vitest passes: ≥ 691 + Phase 1 cumulative new tests; 0 failures.
    - `npm run lint -w frontend` exits 0.
  </acceptance_criteria>
  <done>FE complete. SC-1 + SC-2 + SC-4 all pass via integration test. Plan 08 finalizes spec sync + grep gate.</done>
</task>

</tasks>

<verification>
- FE full suite GREEN, baseline ≥ 691 + new tests.
- `npm run lint -w frontend` 0 errors.
- Manual UAT (referenced by 01-VALIDATION.md §Manual-Only) handed to user post-execute.
</verification>

<success_criteria>
- D-03 (view-mode tabs) + D-04 (URL state) shipped.
- SC-1 modal CRUD + SC-2 optimistic badge + SC-4 search across tags all green via AdminTagsPage integration test.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tag-rules-consolidation/01-07-SUMMARY.md` with:
- Final FE Vitest pass count.
- New page test cases (count + descriptions).
- Any deviations from UI-SPEC §Copywriting Contract (should be zero).
</output>
