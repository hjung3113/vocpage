---
phase: 01-tag-rules-consolidation
plan: 08
type: execute
wave: 4
depends_on: [04, 07]
files_modified:
  - docs/specs/requires/feature-voc.md
  - docs/specs/requires/routing-conventions.md
autonomous: true
requirements: [REQ-admin-pages-wave]

threat_model:
  trust_boundaries: []
  threats:
    - id: T-01-17
      category: I
      component: "Spec drift between docs and code"
      disposition: mitigate
      mitigation: "feature-voc.md §9.4.1 + routing-conventions.md updated in same wave; final rg gate proves zero residual references; ADR / git history excluded per D-10."

must_haves:
  truths:
    - "feature-voc.md §9.4.1 describes modal-based row-action consolidation, view-mode tabs, nested REST paths, keywords[] + match_mode + created_by_name."
    - "routing-conventions.md no longer mentions /admin/tag-rules."
    - "rg `/admin/tag-rules` across code (excluding ADR + .planning + graphify-out + node_modules) returns 0 lines."
    - "All 5 ROADMAP success criteria for Phase 1 verifiable from this commit."
  artifacts:
    - path: "docs/specs/requires/feature-voc.md"
      provides: "§9.4.1 rewrite — single source of admin-tags rule consolidation"
    - path: "docs/specs/requires/routing-conventions.md"
      provides: "/admin/tag-rules entry removed; /admin/tags ?view= ?q= conventions documented"
  key_links:
    - from: "feature-voc.md §9.4.1"
      to: "shared/openapi.yaml /admin/tags/{tagId}/rules*"
      via: "REST URL references match"
      pattern: "/admin/tags/\\{tagId\\}/rules"
---

<objective>
Spec sync (SC-5): rewrite `feature-voc.md §9.4.1` to describe the as-built consolidation, remove the legacy `/admin/tag-rules` entry from `routing-conventions.md`, and run the final `rg "/admin/tag-rules"` = 0 verification (D-10 policy boundary).

Purpose: Phase 1 ROADMAP success criteria #3 + #5. After this plan: a fresh reader of the spec sees only the new world; ADRs/.planning archives still contain the historical references (intentional per D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tag-rules-consolidation/01-CONTEXT.md
@.planning/phases/01-tag-rules-consolidation/01-04-SUMMARY.md
@.planning/phases/01-tag-rules-consolidation/01-07-SUMMARY.md
@docs/specs/requires/feature-voc.md
@docs/specs/requires/routing-conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.08.1: Rewrite feature-voc.md §9.4.1 + remove /admin/tag-rules entry from routing-conventions.md</name>
  <files>docs/specs/requires/feature-voc.md, docs/specs/requires/routing-conventions.md</files>
  <read_first>
    - docs/specs/requires/feature-voc.md (lines 545–549 — current §9.4.1; also read §9.4.6 / §9.4.7 for cross-reference accuracy)
    - docs/specs/requires/routing-conventions.md (full file — line 20 with /admin/tag-rules entry; identify any view-mode convention section to extend)
    - .planning/phases/01-tag-rules-consolidation/01-CONTEXT.md (D-01..D-14 — implementation as built)
    - .planning/phases/01-tag-rules-consolidation/01-04-SUMMARY.md (final BE shape — 5 routes, permission matrix)
    - .planning/phases/01-tag-rules-consolidation/01-07-SUMMARY.md (final FE shape — Modal + tabs + URL state)
    - shared/openapi.yaml (final paths block — for accurate URL citations)
  </read_first>
  <action>
    1. **Rewrite `feature-voc.md §9.4.1`** to describe the as-built system. Required content (Korean, matching surrounding sections' tone):

       - Heading retained: `### 9.4.1 태그 규칙 관리`.
       - Edit surface: `/admin/tags` 페이지에서 `규칙 N건` 뱃지 클릭 → `TagRulesManagerModal` Dialog 오픈. Modal 내부에 add 폼 (chip array keyword input + match_mode select) + sub-table (키워드 목록 / 매칭 방식 / 상태 / 작성자 / 작업).
       - 전체 규칙 보기: 페이지 상단 `태그` / `전체 규칙` 탭 + 키워드 검색(`?q=`). URL state는 `?view=tags|rules` + `?q=`.
       - REST API (D-08): nested `/api/admin/tags/:tagId/rules[/:ruleId][/suspend]` — 5개 엔드포인트, GET / POST / PATCH suspend / PATCH / DELETE. 구 `/admin/tag-rules*` 경로는 제거됨.
       - Schema (mig 024): `tag_rules` columns = id, tag_id, kind, keywords text[] NOT NULL, match_mode text NOT NULL DEFAULT 'keyword', suspended_until, created_by uuid NULL REFERENCES users(id), created_at. `pattern` column 제거됨. List 응답에는 `created_by_name`(`users.display_name` LEFT JOIN) 포함.
       - 권한 (D-13, ADR-0004 §irreversible 준용): GET = Admin/Manager/Dev, POST/PATCH = Manager+, DELETE/Suspend = Admin only.
       - Optimistic update (D-11): create/delete 시 admin-tags 캐시의 `rule_ref_count` ±1, onError rollback, onSettled invalidate. 레이스는 last-write-wins.
       - Trash 복원 멱등성 유지 (ADR-0005, D-14): vocs 복원 시 tag_rules 재실행 정책 그대로.
       - 본 절 내 모든 URL은 nested 형태로 표기 (`/api/admin/tags/{tagId}/rules`).

       Cross-reference §9.4.6 (태그 마스터 권한) and §9.4.7 (휴지통 복원) where appropriate without duplicating their content.

    2. **Edit `routing-conventions.md`**:
       - Remove the `/admin/tag-rules` line at line 20. The route never had a page; removing the entry aligns documentation with reality.
       - Add (or extend an existing section on admin routes) a note documenting `/admin/tags` URL conventions:
         - `/admin/tags` — admin tags + nested rules consolidation.
         - Query params: `?view=tags|rules` (default `tags`), `?q=<text>` (only meaningful when `view=rules`).
         - URL state is the single source of truth for tab + search; deep links work.

    3. Verify both files render as valid markdown (no broken tables, headings, codeblocks).

    4. Do NOT touch ADRs (D-10 policy boundary).
  </action>
  <verify>
    <automated>grep -c "TagRulesManagerModal\|/api/admin/tags/{tagId}/rules\|/api/admin/tags/:tagId/rules" docs/specs/requires/feature-voc.md &amp;&amp; rg -n "/admin/tag-rules" docs/specs/requires/routing-conventions.md ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "9.4.1 태그 규칙 관리" docs/specs/requires/feature-voc.md` ≥ 1.
    - `grep -cE "/api/admin/tags/(\\{tagId\\}|:tagId)/rules" docs/specs/requires/feature-voc.md` ≥ 1.
    - `grep -cE "keywords text\\[\\]|match_mode text|created_by_name" docs/specs/requires/feature-voc.md` ≥ 2.
    - `grep -c "TagRulesManagerModal\|규칙 N건" docs/specs/requires/feature-voc.md` ≥ 1.
    - `rg -n "/admin/tag-rules" docs/specs/requires/routing-conventions.md` returns 0 lines.
    - `grep -c "view=tags\|view=rules\|?view=" docs/specs/requires/routing-conventions.md` ≥ 1 (new URL convention added).
  </acceptance_criteria>
  <done>Spec describes the as-built system; legacy entry purged.</done>
</task>

<task type="auto">
  <name>Task 1.08.2: Final SC-3 grep gate + cross-cutting verification</name>
  <files>(no source modifications — verification only; create a grep evidence file in .planning)</files>
  <read_first>
    - .planning/phases/01-tag-rules-consolidation/01-CONTEXT.md (D-10 policy boundary)
    - .planning/ROADMAP.md (Phase 1 success criteria 1–5)
  </read_first>
  <action>
    1. Run the SC-3 verification command and capture output:
       ```bash
       rg -n "/admin/tag-rules" \
         -g '!.planning' \
         -g '!docs/adr' \
         -g '!graphify-out' \
         -g '!node_modules' \
         -g '!.git' \
         > /tmp/sc3-grep.txt
       ```
       Expected: 0 lines (file empty).

       If hits remain:
       - For each hit, decide: (a) genuine residual code/spec/test reference → remove now; or (b) legitimate exception → document in `.planning/phases/01-tag-rules-consolidation/01-08-SC3-EVIDENCE.md` with justification (must be ADR / .planning / git-history / graphify-out only — those are excluded by the rg flags above, so a remaining hit means missed cleanup).

    2. Run cross-cutting verifiers in one shot:
       ```bash
       npm run typecheck -w backend &amp;&amp; npm run typecheck -w frontend &amp;&amp; \
       npm run test -w backend | tail -10 &amp;&amp; \
       npm run test -w frontend -- --run | tail -10 &amp;&amp; \
       npx tsx scripts/check-fixture-seed-parity.ts &amp;&amp; \
       npm run lint -w frontend | tail -5
       ```

    3. Write `.planning/phases/01-tag-rules-consolidation/01-08-SC3-EVIDENCE.md` with:
       - Date / commit SHA.
       - Exact rg command used.
       - Output: must read `0 hits (excluded: .planning, docs/adr, graphify-out, node_modules, .git)`.
       - BE Jest pass count (vs baseline 593).
       - FE Vitest pass count (vs baseline 691).
       - Parity script: PASS.
       - Lint: PASS.
       - Map each ROADMAP Phase 1 SC (1..5) to where it was satisfied:
         - SC-1: AdminTagsPage.test.tsx (badge → modal CRUD round-trip).
         - SC-2: optimistic.test.ts + AdminTagsPage.test.tsx (badge increment + author display).
         - SC-3: this evidence file.
         - SC-4: AdminTagsPage.test.tsx (?view=rules + search).
         - SC-5: feature-voc.md §9.4.1 + routing-conventions.md + shared/openapi.yaml all in same PR.

    4. If anything fails, do NOT mark phase complete — escalate to the orchestrator.
  </action>
  <verify>
    <automated>rg -n "/admin/tag-rules" -g '!.planning' -g '!docs/adr' -g '!graphify-out' -g '!node_modules' -g '!.git' ; test $? -eq 1 &amp;&amp; test -f .planning/phases/01-tag-rules-consolidation/01-08-SC3-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "/admin/tag-rules" -g '!.planning' -g '!docs/adr' -g '!graphify-out' -g '!node_modules' -g '!.git' | wc -l` returns 0.
    - `test -f .planning/phases/01-tag-rules-consolidation/01-08-SC3-EVIDENCE.md` returns true.
    - Evidence file contains explicit pass/fail for: BE typecheck, FE typecheck, BE tests, FE tests, parity script, lint.
    - Evidence file maps SC-1..5 → verification artifact.
    - BE pass count ≥ 593 (no regression).
    - FE pass count ≥ 691 (no regression).
  </acceptance_criteria>
  <done>Phase 1 ROADMAP success criteria 1–5 are demonstrably satisfied. Ready for `/gsd-verify-work`.</done>
</task>

</tasks>

<verification>
- All 5 ROADMAP Phase 1 success criteria green.
- Evidence file written; rg gate passes.
- Cross-cutting verifiers (typecheck/test/parity/lint) green.
</verification>

<success_criteria>
- SC-3 (rg grep gate) = 0 hits.
- SC-5 (spec sync) verified by file diff in same PR.
- Evidence document records the proof chain.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tag-rules-consolidation/01-08-SUMMARY.md` with:
- Spec changes diff summary.
- SC-3 grep result.
- Phase 1 ROADMAP completion claim with link to evidence file.
</output>
